# Model scenarios — seven real-life scenes, traced to the record

**What this is:** real moments from the owner's life (given in their words, 2026-07-21) traced end-to-end through the ruled model (`agreements.md`) at the record level — the §8.2 verification step (previously called "the life walkthrough"). Verdict per scene: **FITS** (the model serves the moment cleanly) · **AWKWARD** (served, with named friction) · **MISSING** (the model has no answer). Carries `parked.md`'s watch-list (A2 · A1 · A10 — observe, never test). **Status: CLOSED (2026-07-21, D-079) — owner reactions folded:** S2 blessed (with the owner's phone-browser correction), the bullets-in-one-bit pattern confirmed, deletion-legibility bound as invariant **I-W1**, the bit-title instinct logged to `parked.md` A10. **Final gate: the owner reads this doc.**

**The owner's raw material (their words, compressed):** an article read yesterday + notes on it (in Apple Notes, phone) · Instagram screenshots (aesthetic; one on traditional Chinese medicine) · a downloaded how-to-make-a-mood-board video · a book started, with highlights · a morning thought about virtuous action with a friend ("probably more private — but I'd like it on a board; I'm collecting practices") · morning pages handwritten on the Daylight yesterday · retreat notes to port first (sections, diagrams handwritten + typed, video files — ideally *linked* from Google Drive, not stored) · find-habit today = Ctrl+F in Apple Notes · devices: phone always carried; Daylight + computer at sit-downs.

---

## S1 — Porting the retreat notes *(make-on-canvas · connectors · bookmark · the pull)*

**The scene:** first real sit-down. Desktop or Daylight. The retreat notes come in: sectioned text, hand-drawn diagrams, videos living in Google Drive.

**The trace:**
- Create board → one `board` row (title "Retreat — [name]", `visibility` **private** by default, created stamp). Handwritten title welcome later (hand on top, typed shadow beneath).
- Each section typed → its own **text bit** (`bit` row: type `text`, body; `content` empty here — optional, untitled; a title *can* be written, D-087; face = the body's words) + a `placement` row (this board, dropped position, arrived-stamp = birth: born *on* the board).
- Each diagram → pen session → ✓ Done → one **drawing bit** (strokes as vectors) + placement. The content line is *offered* at Done ("fire ceremony sequence") — never forced; given words make the hand findable.
- The videos → **bookmark bits**: one row each holding the Google Drive URL + a **captured title** read once at save. *Reality note:* an auth-walled Drive URL will yield a junk or empty title → the face falls back to the URL (ruled, §2b "else the URL itself") until the owner types content ("day-2 fire ceremony video") — which this owner would.
- An arrow from a diagram to the section it illustrates → one `connector` row (this board, from-placement, to-placement, when). Arrangement only; feeds nothing.
- Tag the board `#retreat` (tag word row if new + application row on the *board*). Bits born on the tagged board see `#retreat` as a **pre-lit chip** — confirm or flick off, never silent.
- Tap `#retreat` later → **the pull**: every tagged bit *and the board itself*, computed, complete.

**Verdict: FITS.** The founding scene traces clean end-to-end; every act writes exactly one legible record.
**Observed:** ① **`video` is absent even from the "later" media list** (pdf · audio only) — but the owner's *actual want* is a reference, not stored bytes, and `bookmark` serves that exactly. One-line flag for `parked.md`: add video to the later-media note so the absence is chosen, not accidental. ② Watch-list **A1: no trip** — porting existing notes is *arranging*, not writing-downward.

## S2 — The week of tidbits *(the capture reality — the walkthrough's big felt deferral)*

**The scene:** the week as lived: article notes, the TCM screenshot, aesthetic screenshots, the mood-board video, book highlights — **all landing in Apple Notes on the phone**, because the phone is what's always there.

**The trace, honestly (corrected by the owner, 2026-07-21):** in v1 there is no *designed* phone capture (Phase 5, re-blessed twice — F8, audit #12) — **but the deployed site works in a phone browser.** The literal v1 phone path: browser → site → board → *+ note* (type) or *+ image* (photo picker) — real bits, synced. The specific clunk: no share-sheet (screenshot → app-switch → upload, ~6 taps vs Phase 5's ~2), a desktop-sized canvas (drop fine, arrange fiddly), online-only (no-signal moments still belong to Apple Notes), no app icon unless hand-bookmarked. So: **phone = possible, unoptimized; Apple Notes = the speed and no-signal fallback**; the sit-down port (paste text → bits via Phase 2's quick add, screenshots → image bits, the mood-board video link → a bookmark) remains the designed flow.

**Verdict: AWKWARD — by explicit, twice-re-blessed design. ✅ BLESSED a third time by the owner (2026-07-21), with the picture concrete** ("I'm just gonna have to compose things on my own is what I thought" — their model already matched reality). The return test's corpus (retreat notes + seeded cluster) doesn't depend on phone capture; the plan holds.
**Observed:** bulk-bringing 10 book highlights = 10 paste-✓ acts — real friction, known and demoted (§2e: splitting is a later nicety). Named here so its demotion is also felt.

## S3 — The virtuous-action thought *(privacy · birth-on-board)*

**The scene:** a morning thought — virtuous action, involving a friend, "probably more private" — wanted on the practices board.

**The trace:** at the sit-down (phone can't capture in v1 — see S2): the practices board (private by default — a locked room). The thought typed → text bit **born `visibility: public`** (the flat default) + placement. The owner, feeling "more private," one-taps it private — offered at creation, changeable anytime.

**Verdict: FITS for v1** — nothing is visible to any other human until sharing ships, and the locked room already contains it regardless of the bit's flag.
**Observed — this scene is A3's named future test case:** a private-ish thought, born public by default, on a board that might *someday* be published. At that future publish, the preview must catch it (one card among many) or the owner flips it private beforehand. If publish-previews ever feel like nervous sweeps, that is **exactly A3's re-entry firing** (contextual bit-privacy: born in a private room → born private). Recorded so the sharing phase inherits a real test, not a hypothetical.

## S4 — Morning pages on the Daylight *(the pen · the typed shadow)*

**The scene:** yesterday, real: handwritten morning pages on the Daylight.

**The trace:** a Morning Pages board (private). Pen → write the page → ✓ Done → **one drawing bit** (the session's strokes, vectors) + placement. Next page = next session. Content line offered, almost certainly skipped mid-flow — so the ink is **unsearchable by words** (ruled: ink carries words *optionally*, P11). Ways back that survive skipping: the board itself, the date (the ledger), any tag. F4 blessed exactly this ("I'd mostly be drawing elements — it's fine").

**Verdict: FITS.** **Watch-list A1: no trip** — handwriting on a canvas is the pen's home; document mode's re-entry is about *typed* downward flow, which this scene never touches.

## S5 — "Where was that TCM thing?" *(find · the bare image · call-in)*

**The scene:** weeks later: "that Chinese-medicine screenshot… where is it?" Today's habit: Ctrl+F in Apple Notes.

**The trace:** find runs over **faces**. The TCM screenshot is an **image bit** — its face is its content line *if the owner told it words* ("TCM — spleen & worry") — else **nothing**. Untold, it is invisible to text search; the honest ways back: find-by-type (all images — free, no tag needed), its board, its tags, or the ledger by date. Told, it's findable forever by those words. If found and wanted on a "medicine" board too → **call in** → a second placement row; one bit, two boards, edit-once-everywhere.

**Verdict: AWKWARD — and known.** The model rules content optional (P5, rightly); this owner's *actual consumption is screenshot-heavy*, which makes the offered-content-line at image-drop **the single highest-leverage habit surface in the product for this owner**. The build must make the offer effortless (one tap to skip, dictation-friendly) — a UI-emphasis note, not a model change.
**Observed:** mechanical text-*extraction* from screenshots (OCR) would dissolve this friction someday — but it sits on P1's boundary (the system reading meaning). Not proposed; logged as an observed future question only.

## S6 — The two-device day *(compose online · the wifi drop · last-arrival)*

**The scene:** phone in the pocket all day (S2's inbox); evening sit-down — Daylight at a café, desktop at home, same boards.

**The trace:** both compose devices are online clients on one database — same board, live, no sync layer. Café wifi drops mid-edit → the save **fails visibly and retries from memory** (I-D3); nothing is durably queued; closing the tab loses the unsaved edit — a named, accepted shape. Desktop later edits the same bit the Daylight touched at lunch → **last arrival wins, whole record** — silent, accepted (§2d), essentially unhittable for one author in one chair-at-a-time. Deleting the tag `#astro` on the Daylight while the desktop tags with it → the tombstone check fires the **keep-by-default prompt** (§2h) — nothing silent, either direction.

**Verdict: FITS.** The tiny-by-construction conflict surface matches this owner's actual pattern (serial sit-downs, not parallel editing).

## S7 — Cleaning up, and the way out *(un-place vs trash · restore · export)*

**The scene:** the retreat board accumulates test scribbles and a duplicate photo; one section bit turns out to belong on the practices board instead; end of month — "is all of this really mine?"

**The trace:**
- The duplicate photo → **trash** → the bit vanishes from *every* board (its cards and arrows hidden, not destroyed); restore next day brings all of it back exactly (§2g). Emptying trash asks first, then destroys totally (placements, arrows, applications, media).
- The misplaced section bit → **un-place** from Retreat (departure stamped; membership row kept — its travel shows it was there) → **call in** on Practices. It carried one arrow → the un-place **asked first** (P12 proportional).
- Export → one tap → every truth row + every file, including travel history and vocabulary (I-G1). Scheduled Phase 1, before real notes land (F5).

**Verdict: FITS — with one build requirement the trace makes non-negotiable:** *un-place* ("remove from this board") and *trash* ("delete the bit everywhere") are different acts with different blast radii, and the UI must make the difference **unmistakable at the point of tap** — the model is clean here, so the only way to lose data is a muddled menu.

---

## Watch-list results (parked.md A2 · A1 · A10)

- **A2 (pairwise link): no trip.** Every relatedness in the owner's material had a natural shared room or word (diagram↔section: same board; TCM↔article: a tag). No board-of-two, no single-use tag.
- **A1 (document mode): no trip.** The owner's long-form is *handwritten* (S4) or *pre-existing* (S1 port). Typed-downward-composition never appeared.
- **A10 (title gate): no trip** in the scenes — long text bits here are highlights and section notes, and first-words genuinely abstracts them. *But: the owner reached for a bit-title three times in conversation during review — logged to `parked.md` A10 as evidence. The gate still fires on real-use display, not expectation.*

## The punch-list *(pending owner reactions — nothing here reopens the model)*

1. ~~Owner re-bless~~ **✅ S2 BLESSED (2026-07-21):** phone = browser-possible but unoptimized until Phase 5; Apple Notes stays the speed/no-signal fallback; the sit-down port is the designed flow.
2. **Build-emphasis notes (no model change):** S5 — the image content-line offer is this owner's highest-leverage findability surface; make it one effortless, dictation-friendly tap. S7 — un-place vs trash must be visually unmistakable.
3. **Ledger touches:** ✅ done — `video` parked with a named re-entry (parked.md **A14**, 2026-07-21: reference-by-choice; stored-video waits for the day a video feels like a bit); S3 recorded as A3's named future test case (in this doc).
4. **Reality note for translation:** captured-title fetch will often fail on auth-walled URLs (Drive, Instagram) — the URL fallback is ruled; make the content prompt natural at bookmark-save.
5. **Observed, no action:** OCR question (S5) sits on P1's boundary — logged only. Bulk-paste friction (S2) — known, demoted, felt.

**Overall verdict: the model survives contact with this owner's real week.** Seven scenes, zero MISSING. The two AWKWARDs are both *chosen* deferrals now felt in the flesh (capture) or known optionalities meeting a real habit (bare screenshots) — exactly the two places real use should be watched during the return test.
