# PROGRESS

Running log of the build. Read every session (after `draft-claudemd.md`); update as work happens. Newest notes on top within each section.

_Last updated: 2026-08-13_

## Status

> **📍 What's next lives in `ROADMAP.md`** (the canonical, reordered sequence — D-053). Read that first for *what to build*; read this section for *where we are right now*.

**📍 NOW (2026-08-13):** the app is **live and daily-usable** at `world-website.vercel.app`. Since the last status: **GATHER is real end-to-end** — the `[[` picker + chips + peek (D-110) *and* the backward "gathered into" on the note's page (D-112); **multi-select** shipped (2a–c); **mobile foundation** (pinch-zoom + readable phone open, safe-areas, fit-to-screen); **writing became a first-class track** — `/write` (born-on-first-content) + open-from-board + touch handles, then **board-creation ergonomics** (look-then-place spawns · 400px born-width + auto-widen · empty-note evaporate · paste-text→note, D-111), then **the note's page table-stakes** (trash · editable title · dates · document treatment, D-112), then **"inbox" renamed → "notes"** — a first-class receptacle beside boards on home, with place-on-a-board at write-end (D-113). Native dialogs → quiet in-app confirms; board-surface decomposed; lint 0 errors. **The aesthetics phase runs in parallel** (`aesthetics-phase.md` — card papers washi/linen/vellum DESIGNED + owner-approved as studies; the north star: bold, ONE dominant direction, owner leaning indigo/Gzhel — awaiting her pick + images). **Next: PHASE O (reorganize) then the F-queue — the live plan + full ordered feature queue is `organize-phase-plan.md` (2026-08-20), read it first.** Still open: publishing app layer (no way to publish yet — all private) · empty-the-trash · nightly backup (needs the owner's GitHub secret). *(Bullets below this line are history.)*

**✅ Compose board MVP: DONE, deployed, and validated on the Daylight with a real stylus** (text reshape+scale, image drop/resize, handwriting/pen — all as movable bits). Live at **world-website.vercel.app**. It's a **localStorage prototype** (per-device, no cloud, no login, currently **public** — no real/sensitive notes on it until the login wall ships in Phase 1).

**Plan reordered (D-053)** around the owner's north star — *"Obsidian, but an interactive canvas."* Sequence: **1) cloud + login + backup → 2) knowledge layer (tags/backlinks/forward-links/find/topic-pages — do it beautifully) → 3) graph → 4) richer boards + growth mechanic → 5) phone capture → 6) browse feed → 7) privacy gradient.** Success metric = *"do I open it unprompted after ~3 weeks to connect/develop ideas?"*

**Right now:**
- **🚧 STAGE 2 (THE PORT) — 2b DONE ON CLOUD, PROVEN (2026-07-22, D-089).** Owner provided `SUPABASE_DB_URL` (session-pooler, port 5432). **The proven schema is now live on the cloud project** (`hfmcxsplktlfihypghpe`): old 7-table prototype schema dropped → proven migration applied clean → **attacks re-run green on cloud (25 refused · 12 held)** + all 10 scenario checkpoints passed on cloud → test rows wiped (empty notebook, seed vocab intact: 4 tags, 5 subtypes) → `public`/`private` storage buckets created → owner login confirmed. *(Done via direct psql; the staged `scripts/apply-to-cloud.sh` produced no output on invocation — syntax-clean, cause TBD, non-blocking — fix before reuse.)* **✅ 2a (login wall) + 2c core (compose on real data) DONE + TESTABLE (2026-07-22).** The localStorage prototype (`app/compose`) is removed; `/` is the auth-gated home (your boards); `/board/[id]` is the real compose surface carrying the proven UX (tiptap text, react-rnd drag/resize, perfect-freehand pen) onto real `bit`+`placement` rows through the one door (`lib/db`), optimistic state + debounced cloud writes. **Verified:** `pnpm typecheck` + `pnpm build` clean; **`scripts/test-port.mjs` green against the live cloud** (RLS wall · board private-default · bit public-default + face computed · board_cards view · home · move+edit persist · un-place-keeps-travel). Dev server runs on :3000. *Font-zoom deferred (no schema home; width-reflow kept).* **▶ NEXT increments: 2d image upload (media pipeline + storage RLS) · the optional title UI (D-087) · content-line offer (S5) · I-W1 un-place/trash surfaces · 2f export.** Deploy (2e) + two-device test (2g) owner-gated.
  - **⏸ CONNECTIONS FEATURE — reviewed, build PAUSED pre-revision (2026-07-24, D-098).** The big one: **"gather a bit into a thought"** (text as the organizer; a stored `reference`; feeds a **read-only** graph). Fully advisor-reviewed, **NOT built · §6 not actually reopened · schema + dormant table untouched.** **Resume from `connections-review-and-resumption-notes.md`** (the master — holds the resumption procedure, 8 findings [3 blocking: dormant-table fate · graph union-not-swap · export/I-G1], + owner rulings [graph read-only · verb = gather]); draft plan is `connections-build-plan.md`. *(Owner cleared to use the live app for real content meanwhile — additive, data safe.)*
  - **✅ DEPLOYED — world is LIVE (2026-07-23, D-092):** **https://world-website.vercel.app** on Vercel, GitHub auto-deploy from `main`, 3 runtime env vars set; **owner confirmed login + boards on their phone.** Port checkpoint **2e met.** Diagnosed + fixed a build-timing gotcha (env vars must exist *before* the build that inlines `NEXT_PUBLIC_*`; a fresh commit — not "Redeploy" — forces it). Custom domain + the formal two-device test (2g) still open, owner's call.
  - **✅ MORNING INCREMENT (2026-07-23) — canvas "fit" (D-090) + HEIC actually works (D-091).** Two owner-reported pains fixed: (1) getting lost on the infinite canvas → a **`⊹ fit`** button frames all cards + boards open already framed on their content; (2) **HEICs failed on every real iPhone photo** → root-caused (old `heic2any` libheif rejecting HEVC) with hard evidence on the owner's real file, then swapped to **`heic-to`** (proven to convert that file) with a "Converting your photo…" notice. Typecheck + build clean; decoder proven on the owner's `IMG_5207.HEIC` through the app's own served copy. Owner does the final in-app click-through (can't log in as them). **Closes parked C2.**
  - **✅ INCREMENT 2 BUILT + CLOUD-TESTED (2026-07-22) — images · titles · words-offers · layout fix.** All four plan items landed: toolbar now in-flow (can't cover the header) · `lib/media.ts` (decode→downscale 2400px JPEG + 600px thumb, EXIF-aware, 25MB cap, the C2 one-line HEIC message) · image intake via + image / drag-drop / **paste**, optimistic local-URL card → upload both sizes → `createImageBit` (thumb rendered on cards) · storage RLS policy `owner_all_objects` applied on cloud · click-to-rename board title (header inline editor) · **the optional text-bit title (D-087)** + media captions via a quiet ContentLine on selected cards (raw `content` fetched in one extra query — the view exposes only the face) · **the "add a few words?" offer (S5)** after image-drop and pen-Done, Enter-saves/tap-skips. **Verified: typecheck + build clean · `test-port.mjs` 20/20 green on the live cloud** (incl. title→face, body-still-searchable D-088, rename→home, storage-RLS upload, signed URL 200, caption→face). Font-zoom still deferred. **⏸ Paused for the owner's feel-test.**
  - **The plan it executed (2026-07-22, just-in-time):** (1) **Images (2d):** new `lib/media.ts` (client decode → downscale JPEG + 600px thumb, dims; throws the HEIC/undecodable message) · add `storage.objects` RLS for the owner on the private bucket (cloud SQL) · wire `+ image` / drop / paste in board-surface → `uploadObject` → `createImageBit` (already in db) → optimistic card (signed URL). (2) **Board title:** `renameBoard` in db + a small inline title editor in the board header. (3) **Per-note title + content:** an inline title field on a selected text card (writes `content`, D-087) + the "add a few words?" content-line prompt on image-drop & pen-Done (skippable, S5) via `updateBitContent`. (4) **Toolbar overlap bug:** fix `.compose-toolbar` layout so it never covers `← all boards` on narrow desktop. Build order: toolbar fix (quick) → images → titles → content prompt. Verify each: typecheck + build + a cloud integration check + owner feel-test.
  - **✅ HEIC treat done + DIAGNOSED + VERIFIED (2026-07-22):** first cut used `import("heic2any")` → the owner hit "still errors." **Diagnosed before re-fixing (owner's ask):** built an isolated browser harness — `heic2any` **converts a real HEIC fine standalone** (3834B→3090B JPEG, 278ms), so the library was never the problem; the failure was **Turbopack mangling the old UMD package's dynamic-import interop.** **Fix:** vendored the dist to `public/vendor/heic2any.min.js` and load it on demand as a plain `<script>` (the exact mechanism the harness proved), `window.heic2any`; dropped the npm dep; proxy allows `/vendor/`. **Verified on the app's own origin** via `javascript_tool`: real HEIC → JPEG in 195ms. `lib/media.ts` converts before the downscale path; one-line message as last-resort fallback. *(Maintainability note: vendored, so a future heic2any update is a manual re-copy.)*
  - **✅ KNOWLEDGE LAYER — FIRST SLICE BUILT + CLOUD-TESTED (2026-07-22): tagging · the ledger · find · the pull.** *(This jumps ahead into Stage-3 territory at the owner's request — they were "hungry for functionality"; the port's remaining bits, un-place/trash + export, are still owed.)* Built clean: `lib/db/tags.ts` (find-or-create word CI, apply/remove application, `getBitTags`, `listTags`→tag_counts recency-first) · `lib/db/find.ts` (the ledger = empty query, + full-text over the face D-088, + tag filter = the pull, + type; tags attached in one round-trip) · **`TagBar`** on the selected note (tap existing / type-to-create / tap-to-remove, §3c) · **`/find`** page: your list of every bit newest-first, a search box (URL = the query), tag filter chips (tap = the pull). Nav links from home + board. **Verified: typecheck + build clean · `test-port.mjs` extended, all green on cloud** (find-or-create, bit-shows-tag, tag_counts, filter-by-tag=pull, full-text find, traceless un-tag). **Not yet: the tag manager (rename/merge/delete) · tag a board · connectors.**
  - **✅ NEXT 3 OWNER ASKS BUILT + CLOUD-TESTED (2026-07-22):** (1) **a bit's own page** `/bit/[id]` — its face/preview (text body · image · drawing · bookmark), tags (→ /find?tag), the boards it's on now, and its **travel** (bit_travel: board · arrived · left/here-now); linked from every /find result. (2) **different-width pens** — a fine/medium/bold picker in pen mode; width stored **per stroke** (so one drawing mixes widths) in the `bit.strokes` jsonb as `{strokes, sizes}`; `normalizeDrawing()` reads both the new shape and old bare-`Stroke[]` drawings (the owner's 4 existing doodles still render). (3) **wider canvas** — the board broke out of the `max-w-5xl` column to fill the screen (owner: "more of the canvas view" — pan/zoom infinite-canvas offered as the further step if that's what they meant). Verified: typecheck+build clean · `test-port.mjs` green incl. the drawing per-stroke-width jsonb round-trip.
  - **✅ ENDLESS CANVAS BUILT (2026-07-22) — owner confirmed they wanted pan/zoom.** A camera `{x,y,scale}` over an infinite world: the board is now a fixed viewport, cards live in a CSS-transformed `.compose-world` (only cards capture pointer events — empty space falls through to pan). **Drag empty space = pan · scroll = zoom-toward-cursor** (native non-passive listener; clamp 0.2–3×) · a `NN%` button resets the view. The react-rnd `scale` prop is wired to the zoom so drag/resize deltas stay true (the D-030/D-048 gotcha handled); `bounds` removed so cards roam freely. Creation (double-tap/+note), image drop/paste, and the **pen** all map screen→world (`screenToWorld`), so things land where you point and strokes convert to world space on save. Typecheck+build clean; **feel/correctness is the owner's in-browser test (like the pen — can't drive it headless behind the login).**
  - **✅ DELETE/TRASH + EXPORT BUILT + CLOUD-TESTED (2026-07-22, owner away — autonomous):** **I-W1 delete** — a selected note shows two distinct labeled acts: **"remove from board"** (un-place; the bit lives on, travel keeps the leg) and **"trash"** (deleted_at; hidden everywhere, restorable) — never one ambiguous button. Home gets a per-board **trash**; **`/trash`** lists trashed bits+boards (trash_listing view) with **restore** (server actions). Permanent empty-trash/destroy deliberately deferred (nothing is destroyed yet). **Export** — **`/api/export`** returns every record kind as JSON + a signed URL per image (I-G1); "export" link on home. Verified: typecheck+build clean · `test-port.mjs` green (trashed-bit-leaves-ledger, shows-in-trash, restore-returns-it, export-reads-all-9-kinds). *(Full media-zip is a later refinement; deploy stays owner-gated.)*
  - **✅ TAG MANAGER + BOARD TAGGING BUILT + CLOUD-TESTED (2026-07-22, owner away — autonomous):** **`/tags`** manager (`tags.ts`: renameTag/mergeTags/deleteTag) — click a word to **rename** (follows everywhere by id, P9), **merge** two into one (repoint+dedupe by construction, I-R7; source disappears), **delete** with a count that reckons with the frozen (I-T2, "N things + M in trash"); merge/delete confirm proportionally. **Tagging generalized to any thing (§3a):** `TagTarget = {bitId}|{boardId}`; the TagBar now edits a selected note OR the board (a "board tags" bar under the board header) — the pull gathers bit AND board. Nav links to `/tags` from home/find/board. Verified: typecheck+build clean · `test-port.mjs` green (board-taggable→pull-has-both, rename-by-id, merge-removes-source, delete-tag-cascades-bit-survives).
  - **OWNER FEATURE-ASSESSMENT given (2026-07-22):** the remaining basics for a *functional daily notes home*, in recommended order: **endless canvas ✅ → delete/trash (I-W1: remove-from-board vs trash+restore — currently NO delete at all, the biggest hole) → deploy to the owner's domain (it only runs on localhost via the dev server — deploy makes it real + reachable on phone/Daylight; owner-gated) → export (I-G1, the ownership floor).** Then depth: tag manager · tag boards · connectors · graph.
  - **⏳ CONTENT-UX REDESIGN — owner feedback, needs alignment then rebuild (2026-07-22):** the owner found the words/title/caption UX over-built — *two* entry points to the same `content` field (the after-create "add a few words?" bar + the on-card ContentLine) read as two features, and the text-bit title displayed stacked on the body felt awkward. The insight: `content` is ONE "words about this bit" thing (text: optional title beside body, D-087; media: caption) — searchable/referenceable, editable on select, NOT force-displayed on canvas. **Plan (pending owner's one call):** unify to a single on-select words field per card (auto-focused right after image/pen create → the prompt *is* that field, drop the separate bottom bar); canvas shows each bit's natural self; **open Q: show media captions under the card when unselected, or hide (cleaner, still findable)?** Rebuild after the owner answers.
  - **Port punch-list (owner testing, 2026-07-22 — fix in upcoming increments, not now):** (a) **BUG — toolbar overlap:** the `+ note` / `✎ pen` buttons overlap `← all boards` when the desktop window narrows (responsive layout fix, polish pass). (b) **GAP — no board title UI:** no way to set/edit a board's name (schema has `board.title`; needs a small inline editor on the board header + maybe home). *(Expected-not-bugs the owner also noted: image drop = 2d · per-note title/content = next increment · tags + search = Stage 3.)*
- **🏗 TRANSLATION (Stage 1) IS RUNNING (2026-07-21) → D-083.** Substage 1a done: the schema derived fresh from the agreements into `supabase/migrations/20260721000001_init.sql` — every line cited, five foreclosure doors held, boundary log in `deliberations.md`. **✅ CHECKPOINT A PASSED (D-084)** — three chunks toured, advisor-verified, owner-blessed; zero schema changes, all fixes were to explanations. **✅ 1c–1e ALL GREEN (D-085).** **1c mechanical:** migration applies clean (the generated columns didn't surprise), **25 constraint attacks refused · 12 behavior assertions held** (counts current post-D-088; the artifact once mis-stated 24/10) · v1 RLS wall holds · the FOR SHARE race probe reproduced the bug then killed it. **1d faithfulness:** all seven scenes replay line-by-line vs `model-scenarios.md` · export completeness (I-G1) · re-place + orphan-arrow regression guards pass (the tripwire caught two real replay bugs before passing — one became a port build-note: un-place deletes every connector where the placement is *either* endpoint). **1e diff:** every disappearance from the old schema names its ruling (`verification/schema-diff.md`) — nothing silently lost. Ran on **native Postgres 17** (Docker's engine wouldn't boot — pivoted, owner-approved; full-fidelity for 1c–1d, cloud re-proven at 2b). Proof set: `verification/`. **✅ 1f DONE:** SPEC rebuilt from the proven schema · ROADMAP rebuilt in the ruled language (zero retired words, parked items reconciled) · the two completed audit docs retired to `old/` · doc census clean. **1g (advisor independent verification) — RAN IN TWO PASSES (2026-07-22):** verified the migration, proofs, live DB, and the Checkpoint-B artifact — caught **4 communication-layer issues** on the artifact (stale 24/10 counts · a "nine kinds is everything" overclaim · a retired-word `link` chip · a title note that overstepped) — **all fixed** — then on a deeper pass **found a real schema bug: the D-087 search hole (D-088)** — titling a text note dropped its body from search (proven live). **Fixed + re-proven green (25 refused · 12 held).** **A10/title: the owner RE-RULED IT IN (D-087)** — the optional text-bit title is in, built at the port (this supersedes an earlier same-day "gate held" note). **The five routed defaults: owner-blessed.** **The D-087 doc sweep is now complete** (agreements §2b/§2f · lexicon · invariants · parked · model-scenarios); **the advisor's plan tightenings 1–8 are folded into Stage 2/3** (split 2a login · no-signup line · the pen named in 2c/2g · the content-line offer in 2c/2d · subtype chips placed in Stage 3 · quick-add early · boardless-bit path named · the pull/ledger presentation-intentionality entry gate). **▶ 1g COMPLETE (2026-07-22) — zero unexplained findings.** The advisor ran checklist items 4/6/7: old-schema diff **passes** (every disappearance names its ruling), retired-words **clean**, and the contradiction sweep found **three time-skew staleness items** — D-087/D-088 hadn't propagated to SPEC §2 · `schema-diff.md` · ROADMAP's not-coming footnote (the downstream docs I updated the model but forgot to re-sweep) — **all three fixed**, plus a flagged non-blocking Phase-6 phrasing note. The advisor's summary: *every finding this round was time-skew, not substance — paperwork lag, not design error.* **▶ STAGE 1 CLOSES on two remaining acts:** (1) the advisor **re-fetches the artifact once** to confirm the 25/12 renumber (verifying, not trusting, the claim); (2) **1h — Gargi signs the page.** Then Stage 2 opens with the cloud pour.
- **🚶 LIFE WALKTHROUGH DONE (2026-07-21) → D-079 / `model-scenarios.md`** — seven scenes from the owner's real week traced to the record: 0 MISSING, 2 AWKWARD (both chosen + blessed). **§8's verification is complete; TRANSLATION is next** (SPEC rebuilt · schema derived fresh · ROADMAP reconciled — honoring `parked.md`'s foreclosure notes + the walkthrough's build notes). Final gate: the owner reads the walkthrough doc.
- **🔒 Hardening pass DONE (2026-07-20) → D-071** — a record-level rigor audit of `agreements.md` (owner's ask: *"does this conceptually hold — a solid thing that stands on its own, a complete piece of work"*) closed **nine** ambiguities/contradictions and threaded the rulings into the agreements + `lexicon.md`. The conceptual model is now record-level *consistent*, not just closed. Remaining per §8 unchanged: **independent audit → life walkthrough → translation → port.**
- **🧩 Model talk-through with the owner (2026-07-19) → D-059–D-064** — the shape confirmed in the owner's words (bits unnamed; names belong to boards + tags), tap-a-tag = automatic accumulation (name pending), placement ≠ link, bit→board links welcomed, one unified connect picker, the typed-shadow rule (doodle legends + handwritten board titles), every-bit-gets-a-page. **Blocked on the owner's three picks** (see Needs owner) — then: plan-doc/datamodel sweep + `model-scenarios.md` (the flows = the test).
- **🧭 Vetting process adopted (D-058) → `draft-vetting-technical-layer.md`** — before the port, we run: **promise audit** (philosophy → planned pieces; anything unmapped = missing) + **scenario walkthrough** (6–8 real owner moments traced through exact rows) → owner reviews the punch-list → model fixed via migration → *then* the port. Every "done" now names its proof level (L1/L2/L3, ledger in the doc).
- **Phase 1 FOUNDATION set up** (2026-07-19): cloud Supabase project `hfmcxsplktlfihypghpe` created; schema migration applied to cloud + all 7 tables verified; new-format keys (`sb_publishable_`/`sb_secret_`) wired into gitignored `.env.local` + verified; owner account `gargibala777@gmail.com` created via `create-owner.mjs`. **Remaining Phase 1 = the PORT:** wire compose to load/save from Supabase (bits/placements) scoped to the logged-in owner (auth-gate /compose), create `public`/`private` storage buckets + move media to Storage, set the same env vars on Vercel + redeploy, GitHub Actions nightly backup. Privacy built **gradient-ready** (D-056) — routing public-capable, don't hard-wire whole-site login.
- ✅ **Phase-2 knowledge-layer research DONE** → `research-knowledge-layer.md` (cited web research; the design input for the knowledge layer). Key calls captured in **D-054**.
- The compose-polish tasks (delete-a-bit, crop, HEIC, rich text, rotation) are **deferred to "later/treats"** per D-053 — real, but not next.

## Build queue

> **Canonical sequence now lives in `ROADMAP.md`** (compose-first per D-035). The capture-first list below is kept for history — it's the *original* ordering before the D-035 reprioritization; follow ROADMAP for what's next.

### (historical) CAPTURE-FIRST ordering — the daily loop before the sit-down pleasure; reordered per the external review, F1

1. **Foundation** — scaffolded locally (done). Remaining: **cloud Supabase + deploy** (owner accounts) so a phone can reach it. *Critical path.*
2. **Capture loop** — deployed PWA; capture a **bare fragment** (text + photo), optional quick tags; iOS **Shortcut → capture endpoint** (Web Share Target absent on iOS Safari); **offline outbox**. Real data starts landing.
3. **Browse + resurface** — a *designed* fragment grid (image-forward; presentation is an explicit exception to "no aesthetic decisions") + a few **random old fragments on home**.
4. **Fragment detail + edit + soft-delete (trash) + backlinks + automated export/backup** — data safety from day one of real data.
5. **Tagging** — tap-existing **+ create-new**; topical tags = linkable topic-pages; `kind` optional (set at tending); tag-filter on the grid.
6. **Pen-feel spike on the Daylight** (throwaway) → **Boards** — collection mode (group fragments, any device) → canvas mode (model-B spatial: drag/resize, **no rotation v1**, text/image/doodle, touch + landscape).
7. **Stage on boards + a boards-by-stage / tending view.**
8. **Privacy tiers** — private / shared-key / public.
9. **Later / maybe** — scoped graph view; in-app calligraphic pen (if spike passes); audio/pdf bits; wrap-box.

## Decisions log

- **D-001 — One primitive: the canvas. → SUPERSEDED by D-018.** (Later corrected to: the *fragment* is the atom; a fragment needs no board.) _(owner)_
- **D-002 — Auth: Supabase Auth, single user.** Browser gets a real authenticated identity so RLS "to authenticated" works verbatim and future privacy tiers slot in; avoids the shared-cookie approach that breaks RLS. _(recommended default, unobjected)_
- **D-003 — Bits are the atom.** A bit (text/image/doodle/audio/link/PDF) is the taggable, dated, retrievable unit; per-bit tagging in v1. Canvas is also taggable, but a tag-pull surfaces bits. _(owner)_
- **D-004 — No "attributes." → SUPERSEDED by D-013.** (Originally dropped stage/kind; later reinstated, placed differently.) _(owner)_
- **D-005 — Dates:** created + edited on every bit and canvas, auto-maintained by a DB trigger. _(owner)_
- **D-006 — Spatial is core**, not a "later/play" layer. Free-canvas drag/place; desktop-primary editing accepted; capture-simple-then-arrange. _(owner)_
- **D-007 — Doodles:** import PNG now; light in-app finger-draw later; never a full drawing app. **Audio:** attach files now. _(owner)_
- **D-008 — Design:** quiet/white/fast chrome, no aesthetic decisions made for the owner; expression from owner's content; layout inspiration = indie-web "vibes" pages. _(owner)_
- **D-009 — Stack:** Next.js + Supabase (local via Docker) + Tailwind + pre-approved libs. Local-first dev; cloud wired later by the owner. _(recommended default, unobjected)_
- **D-010 — Delivery:** installable PWA (phone + desktop). _(owner)_
- **D-011 — Local Supabase not started, full image stack not pulled.** Disk at 97% (~15 GB free); pulling ~6–8 GB while the owner is out risks their machine. Everything else built + verified offline (schema validated on a throwaway Postgres container). _(Claude, no-debt / no-risk call)_
- **D-012 — v1 RLS is owner-only; anon default-denied.** No anon read-public policies yet (nothing is public); they arrive with the privacy-tiers phase in their own migration. _(Claude, matches build sequence)_
- **D-013 — Attributes reinstated, and *placed*:** `kind` (learned/noticed/wondered/theorized) on **fragments**; `stage` (ordered, ~3–4) on **boards**. Both distinct from open **topical** tags (aboutness, many). Supersedes D-004. _(owner)_
- **D-014 — Assemble-a-page = model B:** manually-placed discrete boxes (text/image/doodle), drag/resize/rotate; each text block wraps in its own box. A wrap-around-a-doodle box (model A, **pretext**) is a later, occasional type — not a v1 dependency. _(owner + Claude)_
- **D-015 — Single-user now; multi-user/product a possible later, not designed out.** Supabase Auth + RLS is already the multi-user shape; don't over-build for it now. Softens philosophy's "never a product." _(owner)_
- **D-016 — Media + doodles:** import auto-sizes (aspect-locked) + in-place non-destructive crop; **transparency, not crop, makes doodles look nice** (transparent PNG, else knock out white on import). Light in-app calligraphic pen (**perfect-freehand**) post-v1; a native app is contested only if the web pen genuinely fails. _(owner + Claude)_
- **D-034 — Naming DECIDED: `bit` (kept) + `board` (renamed from `canvas`).** Code + migration renamed and re-validated (build clean; migration applies). The spatial *mode* of a board stays "canvas mode." SPEC prose reconciles canvas→board per-section as we build. _(owner)_
- **D-035 — Near-term REPRIORITIZATION: build the board/compose (topic-home) MVP first**, not capture. Rationale: it's what the owner's mind wants first, it's the soul/differentiator, and it validates the whole loop with real content (the retreat notes). **The capture-flow work is NOT lost** — fully designed + recorded (SPEC §4b, D-025–D-033), deferred until wanted. Fragments still exist (atoms of boards) and can be **created right on the board** or brought in. The two-review capture-first phasing stays as the *eventual* plan. Insight behind this: capture (scattered mode) and compose (focused mode) are two real modes served by two layers — fragment + board — that are one system; we over-focused on capture *plumbing*, not the fragment layer itself. _(owner)_
- **Open question (think more):** is *everything* placed on a board a first-class, findable fragment, or can a board hold some **local/structural** bits (a title, a label) not tracked in the fragment pool? Doesn't block the MVP — build the simple version (things made on a board are bits), refine by feel.
- **D-036 — Freeform comparison VALIDATED the model, and clarified the differentiator.** The owner wants a compose-canvas (Freeform-like) **plus** a knowledge layer where **each element on a board is a first-class, taggable, referenceable bit** pulled from a shared heap (a bit can sit on one *or more* boards, live). Freeform (no bits/tags) + Obsidian (no spatial integration) *glued together* still can't give this — the value is the **integration**, which nothing off-the-shelf does → **building is justified.** _(owner + Claude)_
- **D-039 — Tagging is always optional, never forced — and not everything on a board is a findable entity.** Owner: idle doodles, a title, a corner scribble don't need tags; a real chunk of notes does. Resolution: **you indicate significance by tagging** (no separate "is-this-a-bit" toggle needed for the MVP) — untagged marks just live on their board; the *find/heap* surface defaults to what you've **tagged/kept**, so idle marks don't clutter it. (Matches JSON Canvas's `group`/structural insight.) The tag UI is the **Obsidian layer** — it comes *after* the compose MVP, so nothing is forced in the MVP by construction. _(owner)_
- **D-038 — Freeform observation (owner's research half): text auto-forms a content-sized bounding box that becomes a movable object.** = exactly our **bit as a movable card**: creating text/handwriting makes a bit whose card **auto-sizes to its content**, then is draggable/resizable. Not a "later" step — it *is* the core board interaction. Target UX: create → a card forms around it → move/resize. _(owner + Claude)_
- **D-037 — Handwriting is IN-APP (corrects D-036's "draw in Freeform, import").** Freeform's pen is **iPad-only** (no Mac desktop) and Freeform is **Apple-only** (won't run on the Android Daylight), so it **can't be the cross-device drawing surface**. So in-app handwriting via **perfect-freehand** is the *primary* path — it's the only thing that runs on the Daylight. Validated by the phase-2 **pen spike on the actual Daylight** (that spike now tests our *real* handwriting solution, not a fallback). Import-a-doodle stays available for anything drawn elsewhere. The Daylight is a fast (60Hz) display, not slow e-ink, so web-pen latency should be workable — the spike confirms. **Freeform stays a *feel-benchmark* only, not a device we can rely on.** _(owner + Claude)_
- **D-040 — Text-box default sizing (corrects the first cut of D-038): a text bit opens at a fixed general width (240px), wraps, and auto-grows *height*; dragging an edge changes width and the text re-wraps.** The initial `autoSize` implementation hugged content on a *single line*, so a long thought sprawled into a mile-wide card — wrong. Dropped `autoSize` entirely (removed the field + branch). Two resize behaviors owner named are both served: auto-height as you type; manual width by drag. Verified live in-browser (old 220px note now wraps to 4 lines; new note wraps at 240). _(owner feedback + Claude)_
- **D-126 — Trash a draft on `/write`, and one delete vocabulary everywhere (owner-ruled, built 2026-08-29).** `/write` had no in-place delete — a note is saved the instant you type (born-on-first-content). Added a **trash** beside "saved · open →": a simple confirm ("Trash this note?") → trash the just-born note (**restorable**) → the editor **remounts blank** for a fresh start (a `resetKey` + cleared refs; any in-flight autosave flush reads the nulled id and aborts). **Terminology ruled (owner): one word — "trash" — for this act everywhere**, no separate "discard," no over-explaining the confirm. The app's delete vocab is now: **trash** = the reversible bin · **restore** = bring it back · **destroy / empty trash** = irreversible. Swept: "discard" only ever appeared in this new `/write` code; everywhere else already said "trash." Reuses `trashBit` + the `confirm` dialog; no schema. tsc + build + lint green. _(owner ruling · the loop · Claude)_
- **D-125 — Empty the trash: permanent delete, the app's first irreversible act (owner-ruled, built 2026-08-29).** Trash gains a **bottom**: per-item **destroy** + **empty trash**, each behind a **double confirm** (owner's ask — no undo). **Reachable only from `/trash`**, guarded `deleted_at IS NOT NULL` at the db layer — a live thing can never be destroyed. Implements the model's **already-ruled** invariants: **I-L10** (bit-destroy total — cascade removes placements/tags/gather-ties-both-ways/travel **+ its media files**, via a new `removeObjects` in `lib/storage`), **I-L6** (board-destroy cascade — placements on-it + board-cards of-it + connectors + tag-apps, **no bits**), **I-L2 · I-T2** (placements deleted by empty-trash · confirms count the frozen). `destroyBit`/`destroyBoard`/`emptyTrash` (db) → `destroyItemAction`/`emptyTrashAction` (server) → client `trash-controls.tsx`. **No schema change** (DELETE on existing tables; cascade already proven in `verification/`). tsc + build + lint (0 errors) green; every path traced. Plan: `empty-trash-plan.md`. _(owner ruling · the loop · Claude)_
- **D-124 — Home = your surfaces: boards + notes merged into one home (owner-ruled, built 2026-08-29; H1–H3 of S2).** Home stops being a separate desk **plus** two list rooms. Built (linear; **no schema**): **the desk** (alive on top — `desk-alive.tsx`) + **one surfaces list** (`home-surfaces.tsx`) — kind tabs (all·boards·notes) · **folders ⇄ flat** · **sort** (alive-first · A→Z · newest created · recently modified) · a **name-jump** (shared `lib/jump-match`) · folders **drill in inline** (accordion) · per-row controls dispatched by kind (`surface-row.tsx`: FolderPicker · ★ `pinBoard`/`pinBit` · board-trash / `BitTrash`). A `Surface` unifies board+note (`lib/surfaces.ts`, one shape to sort). The list is **collapsible + remembered** (localStorage, the rail pattern); when the desk is empty it stays open (never a blank home). **Rooms folded in (H3):** `/boards` `/notes` → redirect to `/` (307, reversible); rail → **home · bits**; retired `boards/page` · `notes/page` · `shelf.tsx` · the `jump-to.tsx` box (all dead after the merge — the jump *behavior* lives on via `jump-match`) — **`/group/[id]` kept** as a deep-link. **Deferred (H4, next phase):** the spatial desk (drag · thumbnails · star-to-place · "alive = on the home board"). **Proven:** tsc + build + lint (0 errors, no new warnings) green · sort/partition logic **8/8** · every control traced. Plan: `home-surfaces-plan.md`. _(owner ruling · the loop · Claude)_
- **D-123 — Search language + the ⌗ filter panel (owner feel-test → built, 2026-08-29; follows D-122).** First feel-test of Search asked for exact matching + Google-style tricks, and a clearer filter. Built, all **client-side** (instant, no schema): a **search language** in the box (`lib/search-query.ts`, parse → compile matcher) — **whole word by default · `word*` = starts-with · `"phrase"` · `-exclude` · two words = AND** — with a quiet **? tips** popover (`search-tips.tsx`); and a collapsible **⌗ filter** panel (`search-filters.tsx`) that moves **kind + tags** off the front and adds a **date range** (on *created*), a dot marking it active. **Defaults (owner-informed):** date = created · `*` = prefix-only · the language is Search-only (Jump-to stays a plain title match). **Why client-side not Postgres FTS:** the DB stems ("run"="running") — the opposite of the exact matching asked for. **Proven:** matcher unit-checked **12/12** (whole-word not mid-word · prefix · phrase order · exclude · AND); tsc + build + lint (0 errors) green. Plan: `find-and-search-plan.md` §6b. **+ Jump-to made consistent (same day, owner ask):** its title match went substring → **word-start-per-word** (`jump-to.tsx`) — "clim" finds "Climate" never mid-word, "clim pol" needs both — no operators (it's a name-jump, not content search); checked 6/6, build green. _(owner feel-test · the loop · Claude)_
- **D-122 — Two kinds of looking: Search (was "find") trimmed to content + the shared Jump-to (owner-ruled, built 2026-08-29).** The owner separated *looking* into two acts (full plan: `find-and-search-plan.md`): **Search** = the broad look — full-text over bits + notes' content, across everything; **a board never appears** (no content of its own). **Jump to** = the targeted look — an instant title filter on a list ("Jump to a board…" / "…a note…"), titles only, a flat list of matches with each item's folder in grey. **Filter** (tags/kind) stays a separate refinement, unchanged. **Built:** boards dropped from Search (deleted `findBoards`/`BoardFindRow`, `SearchKind`/`SearchItem` narrowed to bit|note); **`find` renamed → `search`** (route `/find`→`/search` + redirect, nav label, `lib/db/find.ts`→`search.ts` with `find*`→`search*`, the three `/find?tag=` links) — the shared db-module keeps `bitsFromSource` for the source view; one shared `components/jump-to.tsx` mounted on `/boards` + `/notes` (home inherits it later — reuse: **Search has one home**, **Jump-to is one piece**). Trim **agent-proofed** against the real code; `tsc` + `pnpm build` + lint (0 errors, no new warnings) green. **Authorities swept same-session:** `lexicon.md` (find→search entry + jump-to defined), `model.md`; history + `SPEC.md` + code comments left to their own passes (code renamed with the feature). **Open (deferred to home):** the umbrella *noun* for board+note (the home list's Jump-to placeholder). _(owner ruling · the loop · concept→word→docs→code)_
- **D-121 — HARD LINES: a thing never changes type (owner-ruled, built 2026-08-27). ⚠ SUPERSEDES D-118/D-119's "promote by hand" toggle.** The owner: fluidly flipping a saved thing between bit and note is confusing; the two names must each *mean* something. Ruled — **three kinds, fixed at birth, no conversion:** **bit** = one thing you caught/made (a jotted line, image, doodle; later PDF, voice memo); **note** = a document you *write* — to synthesize thoughts / hold longer text-forward writing — that pulls bits in via `[[` and is placeable on a board like a PDF will be; **board** = a space where you arrange things (organizes a topic; e.g. keep drafts on the board for that topic). **Kind is set at birth (catch/jot → 'bit' · ✎ write → 'note') and NEVER changes** — no "make a note"/"note ✓" toggle. To write a piece from saved bits: start a note, gather them in — you author a new document, not convert one. **Storage stays shared** (a note is still `bit.kind='note'` underneath — invisible plumbing; the hard line lives at the concept/UI level; N1–N3 unaffected). **Code aligned:** removed `KindToggle` (bit page + note page) + `setBitKind`; the only kind-writes are the create paths; tsc/lint/build green, deployed. **Parked (don't block):** what `[[` may target (bits now · a board *later*, the A15 shape · notes-in-notes undecided) · whether future PDF/voice memo count as bits or a distinct "document" class. **Onboarding owes:** teach the three by role + concrete usage examples (owner's "I'm only thinking how I'd use it" — generalize). _(owner ruling · the loop · outside-in logic check + code audit first)_
- **D-120 — The bit/note division + two-way board placement (owner-ruled, built 2026-08-26).** The two things divided cleanly: **`/bits` = fragments only** (written notes filtered out — they live in `/notes`; the intake jot still makes a fragment). Then the placement question, ruled by the owner ("two-way is fine; you might want to put your writing back on the board you're in; placing to a board you decide where it goes"): **a note/bit is placeable from BOTH directions** — board-side (the existing side-panel, unchanged) AND from **the thing's own page** (a quiet "place on a board…" in its "on these boards" section, offering only boards it's NOT already on). Reuses the existing `placeOnBoard` action (blind default-spot landing, "fit" frames it — the owner's "decide where it goes") + the `PlaceOnBoard` component — no new logic; the action now revalidates the thing's page too. **Edges traced before ship:** excludes current boards (no re-place) · callInBit revives a departed leg via 23505 (no duplicate) · guarded when nothing to offer · both surfaces refresh. tsc/lint/build green. _(owner ruling · the loop — planned, checked, then shipped)_
- **D-119 — V2+V3: the kind marker · the rooms · the desk (mock B built, 2026-08-25).** The D-118 rulings made real, mock-first (owner picked B). **Schema** `20260825000002_kind_and_folder_stars.sql`: `bit.kind` ('bit'|'note', CHECKed — unruled kinds refused) + `shelf_group.pinned_at` (folders alive too) — throwaway **16/16** → backup → cloud (83 bits defaulted 'bit'; counts unchanged). **App:** ✎ write **births notes**; a **kind toggle** on a text bit's page ("make a note" ↔) so pre-marker writings promote; **the geography**: `/bits` = the browse surface (was /notes; /inbox redirect updated) · **`/notes` = the notes ROOM** (a notebook index: title · opening words · date) · `/boards` = the cabinet's complete Shelf (was home's) · **home = THE DESK** (mock B): alive tiles (boards + notes, kind-labeled, touched/edited meta, newest star first) + folder chips (starred first, counts, tap→contents beneath, name→the folder page) + the cabinet rail; **"alive" is the UI word** (★ tooltips; the old "pinned" language swept). Folder ★ toggles on /boards. **Named, not silent (the D-117 lesson): the rail is home-only so far — the everywhere-rail (owner's ruling) is the next step**, with the per-page header slimming. tsc/lint/build green; deployed. _(mock-first · the loop · Claude)_
- **D-118 — The vision session: the dream re-centered, the product ruling, "alive," the pieces-terminology, the language book (owner + Claude, 2026-08-25).** The owner asked to go back to intentions before any more building. Out of it: **(1) the product ruling** — philosophy's "it is not a product" superseded, eyes open: this becomes **a product = many single-resident homes** (the never-list — collaboration/AI/analytics/audience-machines — unchanged); the owner wants to live in it, share from it, screenshot it ("the Are.na crowd deserves better"). **(2) "Alive"** defined: what's top-of-mind now, a status the OWNER's hand places across kinds (boards · notes · maybe tags) — the landing shows alive things first (the desk/cabinet principle: home = curated desk; the sidebar = complete cabinet). **(3) The terminology found in the founding doc:** raw → fragments → **pieces** = jots → **bits** → **notes**; notes are first-class beside boards (the marker migration + surface renames queued; lexicon flagged). **(4) `vision-and-language.md` created** — the language book (what-it-is, the multi-mind, the problem three ways, the gradient story, inspiration×clarity, positioning, phrase bank) — Claude-drafted, owner re-voices. **(5) philosophy.md updated** to what's true (product · alive · design-by-choosing · privacy marked under-active-rethink). **Home rebuild proceeds mock-first on the desk/cabinet principle.** _(the owner's vision · Claude scribing)_
- **D-117 — O1b, the shelf FINISHED after the owner's "your work is so incomplete" (2026-08-25).** The owner was right, and the miss is named: O1's build quietly narrowed scope — shelf mechanics shipped, but the promised three-tier layout didn't, groups were boards-only against the owner's folders-across-both ask, and a group had no page. **Fixed, all three:** (1) **home is ACTUALLY the three-tier layout** — rooms big (boards + "+ new board" beside the heading · notes + "✎ write" beside it) · lenses top-right (find · graph) · housekeeping a quiet footer (tags · sources · trash · export · sign out); the flat ten-link nav is gone. (2) **folders cut across kinds:** migration `20260825000001_shelf_bits.sql` (`bit.group_id`, same ON DELETE SET NULL physics) — throwaway-proven **12/12**, cloud-applied (backup → atomic → verified; 21/83 unchanged); the same quiet **group picker on every note card + row**. (3) **a group OPENS:** `/group/[id]` — its boards and its notes together (the folder view; group names on home are links). tsc/lint/build green; deployed. **Lesson recorded: scope narrowing must be surfaced at the loop's clarify step, never silent.** _(owner's call-out · Claude)_
- **D-116 — O1, the shelf: groups + pins on home and notes, APPLIED to cloud + deployed (the loop end-to-end, 2026-08-25).** Built entirely from the GitHub working copy (Documents locked — strike two; the durable fix still owed). Owner's clarify rulings: quiet per-row group picker (groups exist because boards name them) · pins on boards AND notes · owner-ordered groups. **Schema** `20260822000001_shelf.sql`: `shelf_group` (name unique-per-owner · ordered `position` · owner RLS) + `board.group_id` (ON DELETE SET NULL — a deleted section strands nothing) + `board.pinned_at` + `bit.pinned_at` + `home` recreated (b.* carries the new columns). **Proven on a throwaway 10/10** (`run-shelf-native.sh`, repo-root-relative so it runs from any checkout), then **cloud: fresh backup → atomic apply → verified live** (table+policy+columns+view; 21 boards · 83 bits · 75 placements unchanged; grants auto-defaulted, anon RLS-blind). **App:** `lib/db/shelf.ts` one-door · home = the Shelf (★ pinned → your groups ↑/↓ → ungrouped; picker with "+ new group…" · ★/☆ · trash) · notes float pinned-first with ★/☆ on cards + rows · `shelf_group` in the export lockstep (I-G1). tsc/lint/build green; deploy success. _(the loop · owner's key at the gate · Claude)_
- **D-115 — O2+ list view · O3 gather surfaced (built + deployed, 2026-08-21).** Two small loop items: **(1)** notes gains a **⊞ cards / ☰ list toggle** beside the sort — list rows: type · title (+32px image thumb) · source · "on 〈board〉" (all tab) · date · the same doors (place-on loose-only A20 · trash); composes with tabs/search/filters/sorts. **(2)** Gather stops hiding: the text toolbar gains a **`[[` button** that *inserts the trigger at the caret* — the existing watcher opens the picker (ONE code path; the gather plan's **Daylight touch gate** finally honored everywhere the toolbar shows) — and /write's idle line teaches the gesture ("type `[[` to gather a note into your writing"). tsc/lint/build green each. _(owner ask + the loop · Claude)_
- **D-114 — O2, the first item through the owner's loop: notes = the bit-first view (clarified → planned → code-proofed → built, 2026-08-21).** The organize phase's item loop (plan §5) ran start to finish: owner ruled **full view** (search + type filters) and **three sorts** (newest · oldest · recently edited). Built: `/notes` lands on **loose** (the old page, byte-for-byte the same cards) with an **all** tab (`?view=all`, linkable) — in-memory search over face/content/body/source/tag-words (the ruled A22 pattern), notes/images/sketches filters, the three sorts; on "all," placed bits show **"on 〈board〉" links** and **place-on stays loose-only** (the multi-board door remains parked A20, deliberately); trash everywhere; the jot box on both tabs. One fetch (`listAllBits`; loose = its boards-empty subset — F19 holds). `InboxCard` → client `note-card.tsx`; new `notes-browser.tsx`; page slimmed. tsc/lint/build green. _(the loop's steps 1–8 · Claude)_
- **D-113 — "Inbox" → "notes" + notes as a first-class receptacle + place-at-write-end (owner ruling, built 2026-08-13).** The owner, testing D-112, hit the miscasting: a *written* note landing in an "inbox" reads as mail-to-process, not "where my writing lives" — and asked the sharper question: *"where is the receptacle, the way I can see my list of boards?"* (Their framing en route — "a note is like a text board" — is affirmed as exactly what **document mode** (A1/Plan E) delivers *via the board*; the model's line stays crisp: a note **references** (chips point), a board **members** (placements compose). Categories walked, no model change.) **Built:** the surface **renamed inbox → notes** everywhere visible (route `/notes` + permanent redirect from `/inbox`; nav labels; page heading) — the lexicon updated same-pass (the computed view keeps its technical name `the_inbox`); **home now shows both receptacles** — boards *and* notes (with the loose count + a ✎ write door); **✎ write ends with the finishing act** — "place on a board…" (the notes page's own `PlaceOnBoard`, + an `onPlaced` hook) → "placed on its board ✓" (the in-your-notes link deliberately dropped once placed — it would lie). Supersedes D-100's *naming* only — the loose⇔render-rule model is untouched. `tsc` clean · build green (`/notes` routed). _(owner ruling · Claude build)_
- **D-112 — The note, table-stakes: gathered-into · trash/title/dates on the page · /write orientation · the document treatment (planned with the owner, built 2026-08-13).** The owner's asks ("I can't delete… where do I check a note I wrote… how do I title it") + the owed back-half of gather, per `writing-experience-plan.md` §v1.2 — board cards + inbox were already complete; the gaps were the note's PAGE and `/write`: **(1) Gathered into** — new `listGatheredInto` (reference where `to_bit_id=X`, gatherer joined, trashed gatherers excluded by the render rule; ties survive in rows so restore brings entries back free) → a quiet section on the note's page: every live thought that `[[`-reached for this note, face + when-gathered stamp, **plain links** (a page list — peek stays the chip's gesture). **The join shape PROVEN against the live API before shipping** (read-only script; returned the owner's real Aug-1 gather). One-mechanism confirmed to the owner: card-on-canvas or page, same chip/rows; boards not gatherable (A15). **(2) Trash on the page** — the board's honest on-N-boards confirm → `trashBit` → `/inbox`. **(3) Title on the page** — `BitTitle` writes `content` (D-087, the exact field the board card edits; media gets the caption placeholder). **(4) Dates** — created · edited by the type label (the owner's date-stamp standing wish). **(5) /write** — an optional title (held until the note is BORN on first body content — title alone never births; flushed via `updateBitContent` after create settles) + the status becomes doors: "in your inbox →" · "open its page →". **(6) `.page-editor`/`.page-title-input`** — 17px/1.65 measure-comfortable document treatment on the page + /write (mechanism; the look stays the owner's). No schema change; all acts through the existing one-door fns. `tsc` clean · lint 0 errors · build green · join proven live. _(owner asks · Claude build)_
- **D-111 — Board creation ergonomics: look-then-place · a note-shaped note · evaporate · paste-text (planned with the owner, built 2026-08-13).** The owner's two real-use frictions — new things **stack** ("popping up on top of the previous one") and the text card **receipts** ("a super long receipt") — plus the swept adjacents, all per `writing-experience-plan.md` §v1.1: **(1) `findClearSpot`** — ONE shared look-then-place for every non-deliberate spawn (`+ note` · `+ image` picker · paste · call-in, whose old 6-step cascade *cycled*): hit-test at the thing's size against every card (text heights measured from the **rendered DOM** via `data-pid` — state h is stale by design), step down-right, **prefer fully-in-view**, cap → plain cascade; double-tap/drop/pen untouched (deliberate spots). **(2) A note-shaped note:** born **400px** (supersedes **D-040's 240px** — the owner called it small 3× in real use) and **auto-widens while typing** (receipt-shaped ⇒ +80px steps to 560, then grows down; the owner's own resize sets `userSized` and always wins; same `onChange({w})` persistence path). **(3) Evaporate (A):** a board-born note whose edit ends with no real content (the `/write` `hasContent` test) un-exists — card removed + `abortBitCreate` through the **settled door**; once it ever held content it stays; old notes emptied by hand untouched. **(4) Paste text → a note (D):** ⌘V on the board (guarded: never when an editor/input has focus) → one note, lines→paragraphs, HTML-escaped, at a clear spot, selected-not-editing. **(C) zoom-on-create REJECTED by the owner** (the app grabbing the wheel) — not re-proposed. `tsc` clean · lint 0 errors · `pnpm build` green; feel-test = the owner (five `+ note`s land clear · long typing widens then grows · a hand-narrowed note stays · stray double-tap leaves no litter). _(owner-ruled scope · Claude build)_
- **D-110 — GATHER's app layer: the `[[` picker (organized) · the chip peek · screen-edge-aware popups (built + deployed, owner feel-tested "really good", 2026-08-01).** The G2/G3 deferrals from D-101 realized as pure app code — **no schema change** (reads existing `bit` columns only). **The `[[` picker** (`text-bit.tsx` → new `gather-picker.tsx`): the flat by-`face` list — a wall of "untitled" for faceless doodles/screenshots — became a **smart organized dropdown**: two sections split **by type** (notes, found by words · images & drawings, found by sight as thumbnails), a **smart collapse** (typing narrows each; a section with no matches steps aside — the faceless-media row to a slim "N images — tap to browse" line, never vanishing), lazy-signed image thumbs + ratio-true `DoodleBit` minis, empty-notes excluded. **The chip** (`bitref.ts` + new `bit-ref-view.tsx`, a React NodeView): an image/drawing target renders a **thumbnail inline** (a picture, not "untitled"); tapping ANY chip opens a **peek** by the chip — the target's name + a glimpse (text snippet · image · doodle) + **"open →"** (→ `/bit/<id>`) — **peek, not navigate-by-default** (honors the 2026-07-28 ruling). **Serialization untouched:** `renderHTML` still emits `<span data-ref>label</span>`, so `extractRefIds`/reconcile/`search_tsv`/export are byte-for-byte G2 (the P9 carve holds). **Screen-edge-aware popups** (new `lib/floating.ts` — `computePlacement`): one shared placer — **flip** (open where there's room) · **slide** (never cut off a side) · **cap+scroll** (limit height to the room) · viewport-fixed + portaled — fixes the owner-reported "boxes land half-off-screen, have to zoom out" class; applied to the picker + peek, reused by any future popup. **No new dependency.** Verified: `tsc` clean · `pnpm build` green · new files lint-clean (the 2 pre-existing `text-bit.tsx` react-hooks warnings untouched); owner feel-tested the picker + peek on production. Plan `gather-picker-plan.md` (cold-reviewed, choices locked → **DONE**). **Still owed (rest of G3):** the "gathered into" backward list on a bit's page; the tag-suggest box can adopt the placer if it ever clips. _(owner-directed build + feel-test · Claude)_
- **D-109 — All-bits panel + multi-board (① built + deployed, owner "ok!", 2026-07-28).** The loose-notes column became a **searchable browser of every live bit**, loose-first, with a **loose / this-board / other / all** scope filter — so you drop **any** note onto the current board, which is how a bit comes to **live on several boards at once (the D-036 differentiator), edited once → everywhere** (free: it's one row). **`listAllBits`** (new, shared in `lib/db/inbox`): every live bit + source + tags + **live board memberships via the render-rule identical to `the_inbox`** (excludes trashed boards — review **Finding 1**); **`listInbox` now derives from it** (F19 — one loose definition, no drift). Panel gains the scope filter, **"on N boards"** labels, **lazy-signed thumbnails** (only what's shown — F8), a **client-side no-op** for a bit already here (F13). Bring-in reuses **`callInBit` unchanged** (already multi-board-capable · no-duplicate · revive-departed). Removal made honest: **"remove from this board"** (F15) vs **trash** confirming **"on N boards"** (F16). **No schema change, no DB touch — pure app code; `pnpm build` green.** DB behaviors it leans on are proven (`run-1d` re-place/revive · `the_inbox` render-rule); the panel's app-logic is the **owner's feel-test** (can't log in as them). **Planned + cross-window-reviewed** (`loose-notes-redesign-and-multiselect-plan.md`): Finding 1 **fixed** (render-rule), Findings 2–3 **verified already-handled** in `callInBit`, 4 + smaller folded. **Part ② (multi-select) + server-side paging + a `/find`-vs-panel search-semantics unification: parked next.** _(owner-directed build · Claude)_
- **D-108 — The public (guest) door: read-only anon access to public content, APPLIED to cloud (owner "go", 2026-07-28).** Opens the sharing-phase door `init.sql` framed as commented policies. A logged-out visitor (`anon`) may SELECT — **never write** — `board`/`bit`/`placement`, filtered by the AND-composition: a note shows only if **public AND live AND on a public+live board** (I-P5); unplaced-public never shows (no feed, I-P4); a private card is absent by row-absence, so a public board renders its public cards only. `reference` (the private gather/twin thread), `tag`, `source` are **NOT** exposed. Recursion-safe via three `SECURITY DEFINER` publicness helpers (booleans only, no content). **Proven leak-proof on a throwaway copy** (`run-public-door-native.sh`: the attack fails to reach private/unreachable/unplaced/trashed/departed data six ways · anon read-only · owner intact), then **applied to cloud** (atomic, fresh-backup-first). Verified live: 3 guest policies + 3 definer helpers present; a visitor currently sees **0 boards / 0 bits** — every board is still private, the safe default; **nothing is exposed until the owner deliberately publishes.** Migration `20260728000002_public_guest_door.sql`. **The publish ACT · the public ROUTE · the twin-thread are the next (app-layer) build — until then there's no way to publish and nothing is public.** _(owner "go" · Claude)_
- **D-107 — Per-row ownership: the hardcoded owner uid removed, APPLIED to cloud (owner "go", 2026-07-28).** D-094's RLS predicate `auth.uid() = '298fbf29…'` replaced by **per-row ownership**: every owned table (all 11) gains `owner_id` (default `auth.uid()`, not-null), policies become `auth.uid() = owner_id`; existing rows backfilled to the current owner (the uid's ONE remaining mention — a data value, not a predicate). App code untouched (the default fills `owner_id` on every insert). **Also the exact multi-account foundation** (parked A20 — everyone-their-own-notebook is now a door, not a rewrite). **Proven on a throwaway copy** (`run-ownership-native.sh`: backfill · de-hardcode · owner-isolation · stranger-locked-out · anon-blind), then **applied to cloud**: fresh backup (`backups/pre-migration-…`) → atomic `--single-transaction` apply → verified live (11/11 `owner_id` not-null + default; **0** policies name a literal uid; 24 bits / 11 boards / 19 placements **unchanged**; all owned by the owner; through-RLS the owner still sees all 24/11). Migration `20260728000001_per_row_ownership.sql`. _(owner "go" · Claude)_
- **D-106 — DEEP REVIEW + same-night fix pass (2026-07-27, owner away — autonomous; push held).** A five-dimension multi-agent review (correctness · security · model-gates · craft · docs-drift), every substantive finding adversarially verified by two independent lenses: **11 confirmed · 0 refuted · 25 craft/docs passed to judgment.** **All 11 fixed same-night:** the **settled-create door** (un-place/trash/content waited on an in-flight create — a 0-row update silently lost the act and the card *resurrected*; now every card write routes through `settled()`, and `unplaceBit`/`trashBit` assert a row was touched) · intake + place-on-board **pending-lock** (a rejected server action froze the box forever — try/finally) · `callInBit` **departed-only revive** (a live placement is returned untouched, never yanked to a new spot; optimistic twin deduped) + **liveness guard** (no write lands on a trashed bit/board — I-D1; honest "in the trash" messages; the full §2h FOR SHARE mechanism stays owed) · **addToInbox compensating abort** (a late source/tag failure deleted the just-born bit so retry can't duplicate) · **/export completeness** (`source` + `reference` tables were missing — I-G1's exact failure class, third occurrence, lockstep comment added) · source manager confirms **count the frozen** (I-T2 — live + trash split in `listManagedSources`) · **setSource URL back-fill** (a pasted url no longer silently dropped when the same-named source has none; the *conflict* case → parked A23). **Plus the judged extras:** blur-commit completed with **unmount-commit** (deselect no longer drops a typed tag/source) · source urls **scheme-validated at the write door** (http/https only — guards every href) · page-meta rewritten to what it is (a source-title fetcher: dead bookmark fields dropped, IP-literal/IPv6 blocklist, streamed size cap) · stale-load invalidation + view-face in the column · door-B spread · stale "sticky" comments swept. **Verified:** `pnpm build` green · `run-1d` + source + gather proofs green. **Docs re-synced (the drift findings):** receipts D-104/D-105 written · Status + Needs-owner refreshed (were frozen at 07-21) · ROADMAP "where we are" rebuilt · agreements §2b + lexicon bookmark-face/captured-title swept to D-102 · gather plan target-list swept · parked C3/C6/B5 marked done (retroactive) + **A19–A23 added** (bulk call-in · the multi-board door · drag-to-drop · server-side filtering · URL-conflict surfacing) · hub row for call-in. **Queued for the owner (not done solo):** storage-bucket RLS dashboard verification · the §2h liveness sweep beyond call-in · file-size-ceiling refactors (board-surface 489 · bits.ts 268 · three new ~215s) · image bring-in paint-order · manager/board banner nudges · workspace source-link parity. _(review: 27 agents · ~1.5M tokens · findings in the workflow journal)_
- **D-105 — UX papercut sweep + tags-at-capture + smart source links (owner-reported + two read-only sweeps, 2026-07-27).** The owner reported three intake papercuts and asked for a class-sweep; two parallel read-only audits (stale-state/reset · layout-shift) found the same classes elsewhere; all fixed + deployed (`532684a`, `31cd0cf`): intake **full reset** (box height snaps back — useLayoutEffect, no scrollHeight race; the typed source is *applied*, never dropped; source/tags/note all clear after add — the sticky behavior superseded at the owner's ask) · **tags in the intake** (autosuggest chips, applied on add — `addToInbox` gains `tags[]`) · the board's selection toolbar **floats** (was in-flow — every card-select shoved the board down ~80–100px; layout stability) · inbox "＋ tag" opens **chrome-less** (no grid-row jump) · tag-bar/source-picker **commit on blur** (type + click-away saves — matching the app's other editors) · managers **keep the field open on a failed edit** (typed text survives) · **smart source links** (a source pasted as a bare URL → `normalizeUrl` + `fetchPageMeta` → the source is "Title ↗", fallback the URL, capture never blocked). Deliberately left: the manager status-banner + board save-error nudges (rare, fix has real downsides). _(owner-directed · Claude)_
- **D-104 — CALL-IN: a loose note onto a board, both doors (planned with the owner + cross-window review, built + deployed 2026-07-27; commits `6c40716`, `db89543`).** The capture→arrange loop closed. **Door A (primary):** a collapsible **loose-notes column** on every board — the inbox as a side panel (ONE shared list, gate 4), newest-first, in-memory **search + tag/source/type filters**, click → the note lands at **view-center** (screenToWorld + cascade) and leaves the pile; optimistic like createNote, rolled back on failure. **Door B:** a quiet **"place on a board…"** dropdown on each inbox card → default-spot landing ("fit" frames it). **One function underneath — `callInBit`:** insert with the optimistic id; on `23505` (the partial-unique `placement_bit_once` covers *departed* rows) **revive the bit's old row on that board** — `left_at` cleared, position set, **`arrived_at` untouched** (the owner's date-stamp ruling: full data story kept — first arrival + auto `updated_at`), the client **reconciles** its guessed placement id to the returned real one *before* any debounced move fires (`reconcileId` re-keys pending patches + timers). **No schema change** — the timestamps, the one-row-per-(note,board) rule, and the inbox view all pre-existed; call-in is their app-layer consumer. **Proven:** `run-1d-native.sh` green (re-place reuses the row · arrived_at preserved · duplicate refused) + `pnpm build`. **Plan:** `call-in-build-plan.md` (five safety gates walked with the owner; the cross-window review's 3 findings folded — the reconcile step named, gate-zero un-place-stamps-left_at verified, the connectors-not-restored consequence documented). **Conscious scope:** both doors surface *loose* notes only — the multi-board door (D-036's differentiator) is parked A20, deliberately. _(owner sign-off on plan · Claude build)_
- **D-103 — Source & the full bit: the app built + deployed (Stages 1–3 + the source manager, on the D-102 model; 2026-07-26/27).** The visible realization of D-102 — pure app code on that schema, all pushed to `world-website.vercel.app`, each verified by `pnpm build` (runtime feel-tested by the owner; the DB queries can't be tested without their login). **Stage 1** (`1f1c2f8`): rich text on `TextBit` (a quiet toolbar — bold·italic·lists·quote·link, via `@tiptap/extension-link@3.28`) · `lib/db/sources.ts` + `SourcePicker` (clone of `tags.ts`/`TagBar`, single-select) · the bit page `/bit/[id]` became the editable **workspace** (rich-text body through the **tiptap pipeline** — review finding #6, never raw HTML · editable tags · source picker) · "from [name] ↗" on the bit + board cards. **Stage 3** (`eb75d7f`): the inbox reworked into a calm browse surface (each loose bit by kind + its "from …" + a collapsed "＋ tag" · open→workspace · trash) · `/source/[id]` (everything from a source) · `/sources` reading list · nav gains `sources`. **Stage 2** (`53637b9`): the intake — a sticky "capturing from [source]" (pick-or-create) + a note box + an "as a quote" toggle (blockquote); each add → a loose text bit carrying the source (`addToInbox`/`Intake`); the **final bookmark code cleanup** — `createBookmarkBit` deleted, `"bookmark"` dropped from `BitType` (now text·drawing·image). **The source manager** (`e56db54`): `/sources` made editable (cloned from the tag manager) — `editSource` (name + url; `23505` collision surfaced), `mergeSources`, `deleteSource` (FK `set null` keeps the notes — I-Src4); rename (id-referenced re-label — I-Src3), edit URL (closes the intake URL gap), delete (notes survive, lose the stamp), merge. **No model/schema change** — D-102 already carries the model, so nothing to fold into agreements/invariants. Active plan `source-and-full-bit-build-plan.md` (Stages 0–3 + the manager DONE + deployed). **Owed:** the intake source picker is name-only at creation (URL added via the manager); "place-on-a-board" (call-in) and Gather G2/G3 stay their own tracks. _(owner-directed build · Claude)_
- **D-102 — Source made first-class; bookmark retired (Stage 0 model fix; proven-on-a-copy, then applied to cloud, 2026-07-26).** Migration `supabase/migrations/20260726000001_source_first_class.sql`: a **`source` table** (vocabulary-shaped — `name` + optional `url`, `source_name_ci` CI-unique, owner RLS per D-094) + **`bit.source_id`** (nullable FK, `on delete set null`); D-100's `source_url`/`source_title` migrated into `source` records (name-fallback = `coalesce(source_title, url)`) then dropped; **every `bookmark` bit converted** to a text note (a rich-text link) carrying a source, then the type **retired** (`bit_type_allowed` 4→3, substance branch dropped, reverting D-100's preview relax); `board_cards`/`the_ledger`/`the_inbox` refreshed. Load-bearing order = **convert-before-tighten** (the proof checks **zero bookmark rows survive**). Proven on a throwaway DB — `verification/run-source-native.sh` green (zero bookmarks survive · conversion + name-fallback · `name_ci` dup-refusal · FK set-null on source-delete · grouping exact · `type='bookmark'` refused · owner RLS), then **applied to cloud** (owner sign-off). **Record kinds 8→9** (vocabulary → four: tag · category · subtype_word · source); **bit types 4→3** (text · drawing · image). **Full ruling + full model on paper:** `source-stage0-checkpoint.md`; active plan `source-and-full-bit-build-plan.md`. **Docs folded same-session:** agreements §2a (the pivot — source first-class · "revisit = a tag") + §1/§7 storage map + type row · lexicon (source · sources-list · fourth axis · type-list · code-names · `bookmark` retired) · invariants **Cluster 6 (I-Src1–5; I-S3/I-S4 retired · I-R3 moot · I-R2 bookmark face-branch dead-but-harmless, no function edit)** · parked **A14 re-homed** (video = source/link, not a saved object) · model-scenarios **S10** + S1 reworded · deliberations (the pivot rationale) · SPEC/ROADMAP record-kinds 8→9 + type 4→3. **Supersedes `capture-build-plan.md` (→ `old/`); reverses parts of D-100.** `captured_title` now a dead-but-present column (later cleanup). _(owner sign-off · Claude bookkeeping)_
- **D-101 — GATHER G1: the `reference` table — model applied, docs folded in (owner sign-off, 2026-07-25 → resumes B9's gather half, D-098).** One new table, **`reference`** — a **directed** tie *from* a text bit's writing *to* any bit, **grown from the body on save** (never hand-authored; a derived index of what the writing already says). Owner-scoped RLS; `reference_not_self` + `reference_once` unique; **cascade both ways** on destroy; "the source is a text bit" is the **one app-guard** (no trigger — G1 proves the DB does *not* enforce it, boundary visible). This is the **§6 amendment** — a directed, grown bit→bit fact now exists; the dormant table **stays parked for the symmetric A2 case** (gather does not absorb it); relatedness is now three-way. Recorded as **owner-authority-ahead-of-evidence (D-087 precedent)** — want-driven, the §6 gate did not fire. Proven on a throwaway copy, then applied: migration `supabase/migrations/20260725000002_gather_reference.sql`; proof `verification/gather-proofs.sql` (+ `.out`, runner `run-gather-native.sh`). **Full ruling + reasoning:** `gather-g1-checkpoint-A.md`. **Docs folded same-session:** agreements §6 amendment · §1 Principle-9 carve · §7 storage-map derived-index row · lexicon (gather · reference · gathered-into · retired backlink/pull-in · the three-way line · code-names) · invariants **Cluster 7 (I-Ref1–8)** · parked A2 unchanged + new doors A15–A18 · model-scenarios **S9** · deliberations (the gather rationale) · SPEC/ROADMAP record-kinds count. **Deferred to G2/G3 (app code, not now):** the `[[` picker + chip, "gathered into" + the lazy self-heal, and the one-line `"reference"` add to `/export`'s `TABLES` array; the **philosophy line** is the owner's to write. _(owner sign-off · Claude bookkeeping)_
- **D-100 — CAPTURE SLICE 1: source + the inbox — model applied, docs folded in (owner sign-off, 2026-07-25).** The smallest capture change: two frozen provenance fields **`source_url`/`source_title`** on `bit` (blank = self-made; only a **clip** carries them), the per-type substance rule **relaxed for bookmarks only** (a bookmark may now also hold a preview `storage_path`), and **the inbox** — `the_inbox`, a computed view = every **loose** bit (live, no live board shows it), newest-first (absorbs the parked loose-bits drawer A6; fixes the only-board-trashed crack). Proven on a throwaway copy, then applied: migration `supabase/migrations/20260725000001_capture_source_and_inbox.sql`; proof `verification/capture-proofs.sql` (+ `.out`, runner `run-capture-native.sh`). **Full ruling + reasoning:** `capture-slice1-checkpoint-A.md`. **Docs folded same-session:** agreements §2a (source + preview) · §7 (loose & the inbox, supersedes the A6 deferral) · storage map · lexicon (the inbox · gathered-into neighbor · source · clip · loose · `the_inbox`) · invariants **Cluster 6 (I-S1–4 · I-N1–4)** · parked A6 ✅ absorbed · model-scenarios **S8** · SPEC/ROADMAP views count nine→ten. **Still ahead (Slices 2–5):** the bookmark card, the inbox *page* (a designed browse surface), place/call-in, clips + the "from …" line, the browser save button. _(owner sign-off · Claude bookkeeping)_
- **D-099 — BASIC-EXPERIENCE POLISH: pen colors/sizes/eraser + tag-suggestion collapse (owner-requested, Claude, 2026-07-25).** **Pen (closes parked C7):** per-stroke **color** — a quiet natural palette (ink · indigo `#3b3f72` · cerulean `#2b6f8c` · forest `#4e6b4a` · terracotta `#9c5744` · ochre `#b07d2b`); wider **sizes** (extra-fine 1.8 → extra-bold 20); **eraser** both ways (an undo button + an erase toggle that rubs out any stroke you drag over, in-session). `Drawing` gains `colors: string[]` — **additive in the jsonb** (no schema change; `normalizeDrawing` defaults old doodles to ink; round-trip proven in `test-port`). *(Erasing an already-saved doodle needs a re-open-to-edit flow — parked follow-up.)* **Tags:** the suggestion chips no longer sprawl inline — applied tags stay visible, suggestions **collapse into a focus-triggered dropdown** (`tag-bar.tsx`); the drop-down stays. Verified: typecheck + build clean · `test-port` green (drawing color round-trip). Owner feel-tests the pen/erase on the Daylight. Diagnosis note: the earlier "canvas struggling on Daylight" was a false alarm (selection-mode confusion; canvas + fit work) — no zoom fix built. _(owner-requested · Claude)_
- **D-098 — CONNECTIONS FEATURE PARKED pre-revision (owner, 2026-07-24 → `connections-review-and-resumption-notes.md` + parked.md B9 + hub rows + plan banner).** The gather + reference-graph feature ("pull a bit into a thought," `connections-build-plan.md`) was fully reviewed by the advisor (vision aligned · 8 ranked findings, 3 blocking · 5 revision asks · 15 clarifying questions) and then **parked by the owner before revision** — "retire graph view for later." **Nothing was built; §6 was never actually reopened; the schema and dormant table are untouched; the tag/board graph at `/graph` remains the graph.** Owner rulings banked for resumption: intent stated plainly ("I do want this for sure" — want-driven re-entry, owner's word); **the graph is read-only in v1** (look + click-to-navigate, never act-from-graph); Daylight `[[` acceptable via on-screen keyboard; verb should be **gather** (the "pull in"/THE-pull collision + retired "backlink" are lexicon-grade findings). Resumption procedure locked: revise per the five asks → advisor deep review of the revised text → the feature's own Checkpoint A (reopened-§6 ruling + the philosophy line in the owner's voice) → build. Everything consolidated in the resumption notes; resume there, never from the plan alone. _(owner parking + advisor package)_
- **D-097 — BIT-PAGE CRASH FIXED (ambiguous placement→board embed) + error boundary (owner-reported, Claude, 2026-07-23).** The graph made bit pages one click away and surfaced a **latent crash**: `getBitBoards` embedded `board:board(...)` on `placement`, but placement links to `board` **two ways** (`board_id` + `target_board_id`) → PostgREST "could not embed… more than one relationship" → the bit page threw → the owner saw "page cannot load," and retry re-ran the same failing query (so "nothing loads"). *(Latent since the bit page was built — nobody had opened one; tag-dot clicks → the pull were always fine.)* Diagnosed by reproducing the click-target reads against the cloud (getBit/getThingTags/getBitBoards/getBitTravel/pull) — only getBitBoards errored. Fix: name the FK — `board:board!placement_board_id_fkey(...)`. Added **`app/error.tsx`** (a calm "try again" + logs the error, so a future snag isn't a dead "page cannot load"). New `test-port` guard on the embed. Verified: typecheck + build + `test-port` green (getBitBoards returns the board). _(owner-reported · Claude)_
- **D-096 — GRAPH VIEW, first cut: the WORD web (owner-requested, Claude, 2026-07-23 → `/graph` · `src/lib/db/graph.ts` · `src/app/graph/*`).** Phase 3 opens. **Design dialogue first (the crux):** this is NOT Obsidian's typed-link graph — there are no bit↔bit links; bits connect only *through* shared tags/boards, so **tags & boards are first-class nodes** (the connective tissue), and two bits relate by hanging off a shared tag/board hub. Owner ruled: **three simple two-type graphs** (Words · Rooms · Map) rather than one tangled three-type hairball; **start with Words** (tags ⇄ tagged bits), **whole-web** (small enough; cap when it grows); full Obsidian-style interactions. Built: `getWordGraph` (one join over bit-tag applications → `{nodes, links}`, degree drives dot size, reuses `bitLabel`, trashed bits excluded); an auth-gated `/graph` page; `WordGraph` (**react-force-graph-2d@1.29.1** — new dep, owner-approved; loaded after mount so it never SSRs and the ref works for zoom-to-fit). Interactions: zoom/pan · hover→label · labels fade with zoom · hover-to-trace (neighbors lit, rest dimmed) · click→open (bit→page, tag→pull) · drag · force-settle + auto-fit. Quiet warm palette (words amber `#b5791f`, bits ink), dots sized by connectedness, legend. Verified: typecheck + production build clean (`/graph` route present) · cloud data check (owner's web = **6 tags · 4 tagged bits · 6 links**). **Owner feel-tests the interactions** (auth wall — can't log in). **Fast-follow (agreed):** bit-dots as their content (image thumbs · doodle previews · note first-lines) · the **Rooms** + **Map** sibling graphs · layer-fading between them. _(owner-requested · Claude)_
- **D-095 — CODE-QUALITY BATCH: cleanup + dedupe + one-door + efficiency (Claude, 2026-07-23, owner-requested).** A behavior-preserving quality pass on the review findings, in 6 verified stages: **(1)** deleted dead code (storage `publicUrl`/`PUBLIC_BUCKET`/`isPublic`, a dead CSS handle; kept `admin.ts` as documented server-only infra); **(2)** one ink color (`INK` in `lib/stroke` + `--ink` CSS var, was hard-coded 9×); **(3)** one label helper (`lib/labels` `bitLabel`/`boardLabel` replacing the fallback wording re-derived across find/bit/trash/home — fixes "a image"→"an image"); **(4)** the one-door: bit/board/tags pages no longer touch Supabase directly (new readers `getBit`/`getBitBoards`/`getBitTravel`/`getBitContents`/`listManagedTags`; `ManagedTag` home moved to the db layer); **(5)** split `board-surface.tsx` 511→421 (`words-offer.tsx` + `use-persistence.ts`, pure relocations); **(6)** efficiency — board-load image signed-URLs sequential→parallel (`Promise.all`), the two image uploads parallel, `DoodleBit` stroke→SVG memoized. Each stage gated on typecheck; final: build clean, `test-port` green. **Deferred (honest):** the camera→`useCamera` hook (would take board-surface ~421→366) — it's interactive code I can't feel-test (no login), so left as a feel-test-required follow-up. **⚠️ NEEDS-OWNER note:** the GoTrue **admin auth API** started rejecting the service key with `unrecognized JWT kid <nil> for algorithm ES256` today (a Supabase-side change — likely a JWT signing-key/asymmetric migration; keys are the new `sb_secret_`/`sb_publishable_` format). **The app is unaffected** (owner login + data + service-role data queries all work; RLS lock re-proven via psql claims-simulation: stranger uid → 0 rows, owner → all). Only `scripts/test-rls-lock.mjs`'s throwaway-user creation is blocked; watch whether it affects signups/app auth. _(owner-requested · Claude)_
- **D-094 — OWNER-SCOPED RLS: the signups-open exposure CLOSED at the DB (owner approved + Claude, 2026-07-23 → `supabase/migrations/20260723000001_owner_scoped_rls.sql` · `scripts/test-rls-lock.mjs` · SPEC §3).** The live deploy had Supabase **signups open** while every table's policy was `for all to authenticated using(true)` — so any account could read/write everything (D-093 finding). Owner asked "you can do it." Fix: an additive migration re-scopes all nine `*_owner_all` policies to the **owner's uid** (`auth.uid() = '298fbf29-…'`, resolved from `auth.users`; USING = reads, WITH CHECK = writes) — the proven init file untouched, tightened via `ALTER POLICY`. **Now robust regardless of the signup toggle.** Applied to cloud + verified: **owner keeps full access** (`test-port.mjs` still green) AND **a fresh 2nd account is locked out of BOTH read and write** (`test-rls-lock.mjs`: creates a throwaway user via service role → zero owner rows visible, insert refused → deletes the user). Recorded in SPEC §3 (the old text wrongly claimed "enforced by the absence of any signup path"). *Native-PG proof suite unaffected (it uses role stand-ins, not real `auth.uid()`); this guarantee is cloud-only, proven by the new script.* Disabling signups in the dashboard is now optional hygiene, not the wall. _(owner-approved · Claude)_
- **D-093 — POST-SPRINT CODE REVIEW + 4 BUGS FIXED; one SECURITY item open for owner (Claude + 3 review agents, 2026-07-23).** Ran a measured 3-dimension review (correctness · data-integrity/security · simplicity) via parallel subagents; each verified findings against the code before reporting. Verdict: **code is clean for the sprint** (no TODOs/dead-logging/hacks; service-role isolation, two-target handling, un-place-vs-trash, counts, export, proxy wall all confirmed correct). **Fixed 4 real bugs:** (1) captions could blank/wipe — `ContentLine` now re-syncs to its `value` when unfocused + the on-card caption is hidden while the S5 "add words?" prompt owns it (also settles the caption-vs-title flow); (2) a photo moved mid-upload lost its spot — a debounced `updatePlacement` could fire before the row existed (0-row update returns no error), so `flush` now awaits the card's create (`creates` map + `trackCreate`); (3) a tag word with `%`/`_` attached the wrong tag — `applyTag`'s 23505 fallback now escapes LIKE metacharacters (new `test-port` guard: escaped resolves to itself, naive over-matches); (4) tag add/remove failures were silent — `TagBar` now shows an inline error + rolls back a failed removal. Verified: typecheck + build clean · `test-port.mjs` green on cloud. **⚠️ SECURITY — found + CLOSED (D-094):** confirmed via the auth `/settings` endpoint that **new signups were OPEN** (`disable_signup:false`); since RLS was `for all to authenticated using(true)`, any signed-up account could read/write all data. **Fixed** (owner approved "do it") — see D-094. Deferred latent items (connector-delete-on-unplace, re-place row reuse) parked to their feature. Remaining review tidy-ups (one-door reads, dead-code deletes, polish) offered as a follow-up batch. _(Claude + review agents)_
- **D-092 — DEPLOYED TO PRODUCTION; port checkpoint 2e MET (owner + Claude, 2026-07-23 → https://world-website.vercel.app).** `world` is **live** on Vercel (project `world-website`, **GitHub auto-deploy from `main`**), free `.vercel.app` URL for now (custom domain deferred — owner's call). Owner set the **3 runtime env vars** (the two `NEXT_PUBLIC_SUPABASE_*` + `SUPABASE_SERVICE_ROLE_KEY`); `OWNER_EMAIL`/`OWNER_PASSWORD`/`SUPABASE_DB_URL` stay dev-script-only (login is a **server action**, not a browser Supabase client — verified). **Owner-confirmed working: login + boards, on their phone.** **Gotcha diagnosed with evidence + fixed:** the first builds predated the env vars, so `NEXT_PUBLIC_*` weren't inlined → the proxy's `createServerClient` got blank keys → **every authed route 500'd** while `/login` stayed 200 (it returns *before* creating the client); a plain "Redeploy" re-served the stale build. Fix: **a fresh commit forced a clean build** (proven: `/` → 307, prod build reproduced 307 locally so the code was never the problem). **Lesson (memory saved [[vercel-env-vars-need-fresh-build]]): after changing Vercel env vars, push a commit — don't just Redeploy.** Boundary honored: deployed live, supervised, with the owner. _(owner + Claude)_
- **D-091 — HEIC DECODER SWAPPED (heic2any → heic-to); C2 CLOSED (owner-approved, Claude, 2026-07-23 → `src/lib/media.ts` · `public/vendor/heic-to.js` · `board/[id]/board-surface.tsx` · `globals.css`).** The shipped `heic2any` bundled an ancient libheif that threw `ERR_LIBHEIF format not supported` (code 2) on ordinary iPhone HEVC HEICs — **every real photo failed identically** while a synthetic test file passed. Diagnosed with hard evidence before touching code (owner's real `IMG_5207.HEIC` — 12MP, HEVC, Display P3 — run through the exact vendored decoder in an isolated browser harness → the libheif error). Fix: vendor **`heic-to`@1.5.2** (IIFE, current libheif, wasm inlined ~3MB, loaded on-demand via `window.HeicTo` **only when a HEIC appears**), call it with `{ type: "image/jpeg" }`, delete the old vendored file; `isHeic()` is now one exported helper shared by the decoder path and the UI. Added a quiet **"Converting your photo…"** notice (HEIC decode is seconds, not instant). **Proven on the owner's file both standalone and through the app's own served copy** → valid 1.3MB JPEG; the friendly one-line message survives as the fallback. Verified typecheck + `pnpm build` clean. **Closes parked C2.** Caveat recorded: heic-to's worker uses `eval` — fine with no CSP today; revisit if we harden CSP. _(owner-approved · Claude)_
- **D-090 — CANVAS "FIT" + OPEN-FRAMED VIEW (owner ask, Claude, 2026-07-23 → `board/[id]/board-surface.tsx` · `globals.css`).** The infinite canvas let the owner zoom out and lose their content; the old reset-view button jumped to the origin (0,0) — blank when content lives elsewhere. Added a **`⊹ fit`** button that frames every card (bounds → centered, scale clamped to [0.2, 1], never magnifies past 100%); the `%` becomes a plain zoom readout. Boards now **open already framed** on their content (fit runs on mount when cards exist) — the same "land on blank" bug fired on every reload, fixed at the class, not just the button. Verified typecheck + build clean. **Owner testing the feel** — auto-fit-on-open flagged as vetoable (→ button-only if it feels jumpy). _(owner ask · Claude)_
- **D-089 — PORT 2b: PROVEN SCHEMA POURED TO CLOUD (Claude, 2026-07-22 → cloud project `hfmcxsplktlfihypghpe`).** Owner cleared the reset + provided the DB connection string (session pooler :5432; the `!` in the password URL-escaped to `%21`). Executed: dropped the old 7-table prototype schema (surgical, named objects only — Supabase's own objects untouched) → applied `20260721000001_init.sql` clean → **re-proved on cloud: attacks 25 refused · 12 held, all 10 scenario checkpoints passed** (cloud behaves identically to native PG) → truncated the scenario test rows so the notebook opens empty (seed vocab kept: 4 thought-tags, 5 subtype chips) → created `public` + `private` storage buckets (service-key API) → owner login confirmed present. Cloud outputs: `verification/attacks.cloud.out` · `scenarios.cloud.out`. **Boundary honored:** production deploy (2e) + the two-device test (2g) remain owner-supervised. *(Note: `scripts/apply-to-cloud.sh` ran silently on invocation despite clean syntax — steps done directly instead; script needs a fix before reuse.)* _(owner-authorized · Claude executed)_
- **D-088 — SEARCH HOLE FROM D-087 — FOUND BY THE ADVISOR, FIXED, RE-PROVEN (advisor 1g + Claude, 2026-07-22 → the migration + `verification/` + agreements §2b · lexicon).** The advisor's independent pass tested a suspicion live against `world_proof`: because `search_tsv` indexed only the **face** (content-if-given), and D-087 now lets a text bit carry both a title (`content`) and a body, **titling a note dropped its body from search** — proven: a note titled "My retreat title" with body "equanimity" was findable by "retreat title" but **not** by "equanimity." Same in miniature for a captioned bookmark (its captured page-title fell out of search). This broke "everything findable" (philosophy · payoff-ledger row 1), and hit exactly the notes important enough to title. **Fix (agreed, no disagreement):** the face stays display-only; **search widened** — new `bit_search_text()` immutable fn indexes **content + body + captured-title + URL** (type-agnostic), feeding `search_tsv`. Amended the init migration in place (D-028 precedent — no persistent DB exists yet), **re-ran the full suite green (25 refused · 12 held)** with a **new permanent regression (C6c): a titled note findable by BOTH its title and its body; a captioned bookmark by both; face unchanged.** Also completed the **D-087 doc sweep** the advisor grep-caught (agreements §2b + lexicon still said "locked for text bits") and folded the advisor's **plan tightenings 1–8** into the build plan. **Honesty correction (advisor's point C):** Stage 1 was over-claimed "done" — it is **close, not closed**: 1g's SPEC/ROADMAP/schema-diff sweep and 1h owner sign-off remain. _(advisor found · Claude fixed · owner relayed)_
- **D-087 — OWNER RULING: OPTIONAL TITLE ON TEXT BITS — A10 gate opened by choice (owner, 2026-07-22 → agreements §2f · parked A10 · invariants I-R1).** Across the Checkpoint-B conversation the owner repeatedly and specifically asked for the *ability* to optionally set a title beyond a text note's first line. Ruled in: a text bit gets an **optional** title — **blank by default (the first line stands in — nothing forced)**, writable when wanted. Mechanically it **unlocks owner-writes to `content` on text bits**; the migration already carries the (unconstrained, nullable) `content` column and commented the unlock as zero-DDL — so it's **a switch, not a schema change**, built at the port. **Supersedes** the 2026-07-20 "no title/subject split" ruling for text bits, and **overrides the advisor's hold-the-gate recommendation** (latest ruling wins; the advisor's process point — that this be the owner's explicit re-ruling, recorded, not the author "leaning" — was honored). No proof/schema re-run needed. Checkpoint-B artifact updated to match. _(owner ruling)_
- **D-086 — ✅ TRANSLATION 1e–1f: DIFF, REBUILDS, RETIREMENT (Claude, 2026-07-21).** **1e:** old-schema diff (`verification/schema-diff.md`) — every disappearance names its ruling, nothing silently lost. **1f:** `SPEC.md` rebuilt from the proven schema (describes, never re-derives — rule 8; the ruled AND-privacy replaces the old OR-leak); `ROADMAP.md` rebuilt in the ruled language (grep-verified zero retired words; parked B/C reconciled one-to-one to phases; event-gated A items kept off the sequence; scene build-notes attached); the two completed audit docs (`audit-agreements.md` · `agreements-review-brief.md`) retired to `old/`; hub rows + the three live pointers repointed; **doc census clean** (README aside, a non-governance file). Substance: deliberations 1e–1f section. **Stage 1 now has only 1g (advisor independent verification) + Checkpoint B (owner) left.** _(Claude, per guidelines §3)_
- **D-085 — ✅ TRANSLATION 1c–1d: MECHANICAL + FAITHFULNESS PROOFS ALL GREEN (Claude, 2026-07-21 → `verification/`).** The schema poured into a real Postgres 17 database and every tour promise became an executed fact: **the bare apply is clean** (the self-computing `face`/`search_tsv` columns — the named likeliest-surprise — applied first try); **24/24 illegal writes refused** by their specific error class (incl. twice-tagging, the cross-board arrow, the departed-row re-place A20b); **10/10 behavior assertions held** (face compute/override/revert, the bookmark URL-fallback, "1 in world + 1 in trash" counts, arrow hide/revive, all destroy cascades); **the v1 RLS wall holds** (logged-out sees zero rows); **the I-D1 race probe reproduced the silent-tombstone bug and then killed it with `FOR SHARE`** (3s block measured) — the twice-corrected mechanism now empirically validated. **Environment pivot (technical, veto open):** Docker Desktop's engine would not boot, so the proofs ran on **native Postgres 17 (Homebrew)** with `anon`/`authenticated` role stand-ins — full-fidelity for everything 1c claims; the real Supabase runtime is re-proven at Stage 2b. Substance + the fidelity argument: deliberations 1c section. Proof set kept as the schema's regression suite. _(Claude, per guidelines §2–3)_
- **D-084 — ✅ CHECKPOINT A PASSED (owner + advisor window, 2026-07-21).** The full table-by-table tour, three family chunks, each independently verified by the advisor against the SQL, each blessed by the owner. Score: zero schema changes across three verifier rounds; every fix was to *explanations* (content physics-vs-discipline · the undo-slogan carve into agreements §5 · flush-attach labeled app-half · the storage overclaim). New owner rulings: traceless un-tagging confirmed · duplicate arrows allowed · category twin-refusal confirmed. New proof obligations: re-place row-identity check · orphan-arrow tripwire · un-place atomicity · port-level flush-attach + confirm-builder tests. Substance: deliberations Stage-1a section. **1c (mechanical proofs) unblocked** — disk freed, CLI install approved. _(owner gate)_
- **D-083 — TRANSLATION STAGE 1a: SCHEMA DERIVED (Claude, 2026-07-21 → `supabase/migrations/20260721000001_init.sql`).** The eight record kinds + the dormant ninth as real tables, derived fresh (old migration unread, rule 6); every table/column/constraint cites its agreements §/invariant; the five foreclosure doors held open and commented. Five behavior-visible defaults routed to Checkpoint A (ledger = born-first · home-bump formula · bit-edit doesn't bump home [advisor catch, round 1] · arrowhead on · display full). Substance: the boundary log + Checkpoint-A feedback round in `deliberations.md` (Stage 1a section); code names pinned in `lexicon.md` (code-names section). Proof level: *drafted-with-citations only* — nothing proven until 1c. Awaiting **Checkpoint A**. _(Claude, per D-081/D-082)_
- **D-082 — TECHNICAL BUILD PLAN ADOPTED + the cleverness standard refined + model arbitrage (owner + Claude, 2026-07-21 → `technical-build-plan-stages-and-checkpoints.md` + guidelines §4 + CLAUDE.md hub).** The whole technical build now has its campaign plan: **Stages 1–4** (translation → port → knowledge layer → return test) with substage acceptance tables, **owner checkpoints A–D** (the tour · the replayed week · the two-device day · living), the **verifier's 8-item checklist** (named checks, never vibes), and **plan-just-in-time doctrine** (full detail only where inputs are frozen — Stages 1–2; Stage 3's detailed plan written fresh at its start; evidence: the retired stale `old/draft-plan-tags.md`). **Cleverness standard refined (supersedes D-081's one-sitting ceiling):** as clever as genuinely better, never simplified *for* the owner — every clever choice passes the triple-check (proven · load-bearing · explainable on demand). **Model arbitrage (owner):** Fable plans + verifies every gate; Opus executes builds — safe because the docs are decision-complete. Stage-2 notes folded in: I-W1 delete-UI, HEIC message (parked C2), the prototype import-or-clean question parked to checkpoint C. _(owner + Claude)_
- **D-081 — TRANSLATION GUIDELINES ADOPTED (owner + Claude, 2026-07-21 → `translation-guidelines-conceptual-to-technical.md` + CLAUDE.md hub + agreements §8.3).** The translation step now has its charter (10 rules: translation decides nothing · the three-tier decision boundary · citations both directions · invariants as the checklist · **the owner's scenarios are the test fixtures** · fresh derivation with old-schema diff-must-name-its-ruling · lexicon names everything · one derivation chain · proofs not prose · owner gates on behavior), an 8-step procedure (derive → prove mechanically → prove faithful by scenario replay → diff → SPEC → ROADMAP → independent verify → owner gate), per-artifact structure/goals/acceptance, **and §4 the engineering strategy** (owner ask: "not just rules — actual things"): the named moves — the target-pair pattern · combine-the-pattern-never-the-tables (three vocabulary tables) · state-lives-in-timestamps · the face as a generated column · surfaces as named views · columns-must-earn-existence · exactly one trigger · RLS gradient-ready — plus the wall-outcome: **the schema must be explainable to the owner in one sitting, or it's wrongly designed** (an acceptance criterion). **Owner refinements folded in:** (1) *owner-as-tester* — testable behavior questions arrive as things to try, never paragraphs to imagine; (2) **the model-scenarios read-gate is satisfied** — the doc was built with the owner live, every verdict theirs; translation is unblocked; (3) **multi-word descriptive filenames welcome** — the brief-titles habit had no rule behind it and is retired (memory updated). Roles: fresh session authors · advisor window verifies · owner rules boundaries + the final behavioral gate. _(owner + Claude)_
- **D-080 — DRAFT PREFIXES DROPPED; PRACTICAL TITLES (owner rule, 2026-07-21).** Closed/verified docs shed the `draft-` prefix: **`draft-agreements.md` → `agreements.md`** · **`draft-walkthrough.md` → `model-scenarios.md`** (owner: "walkthrough doesn't make sense" — the doc IS scenarios traced through the model) · **`draft-research-canvas.md` → `research-canvas.md`**. All live references swept (CLAUDE.md hub + routine, ROADMAP, SPEC, philosophy, lexicon, invariants, parked, both audit docs, PROGRESS live sections) and grep-verified; **the D-log's historical mentions keep old names by design** (append-only, the D-074 precedent). `model-scenarios.md` gained its hub-table row; agreements §8.2 marked done. **Standing rule (memory saved): titles say what a doc literally is — practical over abstract; the `draft-` prefix comes off in the same session a doc closes.** _(owner rule + Claude sweep)_
- **D-079 — LIFE WALKTHROUGH RUN & CLOSED (owner + advisor window, 2026-07-21 → `draft-walkthrough.md` + `invariants.md` I-W1 + `parked.md` A10).** Seven scenes from the owner's real week (retreat notes · the tidbit flood · a private-ish thought · morning pages · re-finding a screenshot · the two-device day · cleanup/export) traced to the record level: **0 MISSING · 5 FITS · 2 AWKWARD, both chosen-and-blessed.** Owner verdicts: **S2 blessed third time, now concrete** — with the owner's own correction folded in (phone = *browser-possible but unoptimized* until Phase 5, not "cannot capture"; Apple Notes stays the speed/no-signal inbox); ten-bullets-in-one-bit pattern confirmed (tags/placements bind the whole bit; copy-out when one bullet matters); **I-W1 new** — un-place and trash are never one button; **A10 evidence logged** — the owner reached for a bit-title 3× across review conversations (instinct recorded; gate unchanged — it fires on real-use display). Watch-list A2/A1/A10: **no scene trips.** Build-emphasis notes (no model change): the image content-line offer is this owner's highest-leverage findability surface (screenshot-heavy consumption); auth-walled bookmark titles fall back to URL → make the content prompt natural at save. Substance: the walkthrough doc. **§8 verification is COMPLETE → translation unblocked; final gate = the owner reads `draft-walkthrough.md`.** _(owner scenes + verdicts · advisor-window traces)_
- **D-078 — Post-soundness nit sweep (advisor re-check + Claude's own greps, 2026-07-20 → `audit-agreements.md` §1/§3 + `ROADMAP.md`/`SPEC.md`/`philosophy.md` preambles).** Four advisor nits, each verified by grep before acting: **(1)** CLAUDE.md's "six" — *already fixed in D-077* (grep-proven; the advisor read a stale copy). **(2)** Audit outcomes rows 8/13 flipped ✅ (export was scheduled per F5; F3 was rejected *and* travel now has its v1 surface, D-076). **(3)** Audit §3's *"the model does not have holes"* paragraph — falsified by the independent audit (#4/#5) — now marked a **superseded dated snapshot**, kept as history. **(4)** ROADMAP/SPEC stay stale-by-design until translation, but both now open with **part-stale banners** pointing at the agreements (SPEC's names its §3 privacy as outright wrong — the OR-leak — so no session can follow it blindly); philosophy's preamble pointer updated (the *what* = the agreements, not SPEC). Sweep also caught: ROADMAP's two pointers to plan docs retired into `old/` (draft-plan-tags / draft-plan-knowledge) — re-pointed to written-fresh-at-translation. _(advisor nits + Claude)_
- **D-077 — SOUNDNESS PASS FOLDED: verdict SOUND-WITH-NITS → all findings fixed; three micro-rulings (owner + Claude + verifier subagent, 2026-07-20 → agreements P10/§2a/§2c/§2g/§2h/§3a/§5/§5c + `invariants.md` + `lexicon.md` + `CLAUDE.md` + `philosophy.md` + `audit-agreements.md` §6).** A fresh guarded subagent verified the merged model post-D-076: **no blockers; all 14 closure claims real; rulings mutually consistent; all eight schemas derive.** **Micro-rulings (owner-approved in plain language):** (1) **keep = recreate by name** — the clash prompt's "keep" for a hard-deleted vocabulary word recreates it via the near-dup machinery (surface-identical to "the word came back"); (2) **destroy cascades stated once, total** — bit-destroy takes placements (+connectors), tag applications, and media (bytes + artifacts); board-destroy enumeration gained "and its tag applications"; (3) **canvas call-ins land at the drop point** (center if summoned) — no positionless cards on an arranged board. **The carve ring** (older absolutes never got the absolutism lens): P10 (recoverable = *things*; acts/vocabulary die with P12 confirms), §3a (category beside the two tag records), §2c ("never erased" carved for the one true destroy + dangling §5 ref fixed + (bit,board)→(target,board)). **Invariants:** I-L5b endpoint's *target* (board-cards); **I-L9 new** (connector endpoints share the board); I-L3 owner-scoped; **I-L10 new** (bit-destroy total); **I-G4 new** (only acts apply meaning); I-D1 keep-recreates note; I-L6 + §2h wording fixed. **Sweeps closed:** two surviving "six"es (lexicon computed-surfaces intro · CLAUDE.md terminology norm) — falsifying deliberations' "executed in full" claim, corrected there (*lesson: claimed-complete sweeps need a grep, not a memory*); stale pointers (§2a philosophy note · §2a parked-line · CLAUDE.md philosophy row · audit outcomes row 10). **Philosophy edited with owner authorization:** the global "review everything open" line → the per-board publish preview (first-person voice kept); "link it"→"connect it", "linked"→"connected". **Process note: the owner flagged jargon-overload — decisions are now presented as plain-language scenarios first (standing memory saved).** _(owner rulings + Claude + verifier)_
- **D-076 — CLUSTER 5 (retrieval honesty) RULED — findings #8/#14 closed → 🏁 THE AUDIT MERGE IS COMPLETE, 14/14 (owner + Claude + advisor window, 2026-07-20 → `draft-agreements.md` P3/P7/P12/§3e/§4/§5/§7 + `invariants.md` I-T1–T6 + `lexicon.md` + `deliberations.md`).** **(#8) The reachability floor:** the gap named precisely — the boardless bit had *lookup* (search/find-by-type) but no *encounter* floor, and the naked bit (no board/tag/words) only find-by-type; §7's ledger promised "time" v1 didn't deliver. **Ruled: find with an empty query = the ledger** — every live bit, newest first; zero design, home ruling (D-070) untouched, the feed stays Phase 6 → **I-T1: nothing stored is unreachable** (scoped: things + vocabulary in no-precondition surfaces, count-0 words listed; acts surface through their things — advisor's rescope). Loose-bits filter deferred. **(#14) Completeness made absolute by domain: world · trash · history.** Trash is outside the world; **the pull is complete over the world, no exceptions**; restore re-includes instantly. **Advisor's attack conceded + generalized:** the boundary governs *surfaces only, never what acts reckon with* — a delete-word confirm omitting frozen carriers lies (P12), and a merge skipping frozen rows would let restore resurrect a tombstone reference (a self-inflicted §2h clash) → **destructive confirms count the frozen (I-T2, all destructive acts)**; **vocabulary ops reach every row (I-T3)**; **history** named the third surface domain (render rule = world; D-071 #3's ever-placed = history; I-T4). **Travel gets its v1 surface** (bit page: has-been-on) — committed, not optional: the F3 rejection rested on "travel + pulls + revision IS growth" (D-069), so surfaceless travel would silently unwind an owner verdict (I-T6); board-side history deferred, named re-entry. **(5C) "A tag opens nothing" ruled:** a guest's pull gathers only what is public AND already reachable — tags never widen reachability; *what counts as open* (globally-public vs share-scoped) = the named sharing-phase sub-question. P3 gains the accelerator parenthetical; §7's ledger swept (words→face — the second silent ledger staleness; the ledger is now in I-G1's sweep surface). **Correction on the record: D-074's parking note misfiled guest-pull reachability into the sharing phase; D-072 had parked it for this cluster — resolved here** (principle now, feature with sharing). _(owner rulings + Claude + advisor window)_
- **D-075 — CLUSTER 4 (two devices & collisions) RULED — independent-audit findings #6/#7 closed (owner + Claude + advisor window, 2026-07-20 → `draft-agreements.md` §2d/§2g/§2h(new)/P12 + `invariants.md` I-D1–D6 + `deliberations.md`).** **The reframe that dissolves the "sync problem": the conflict surface is tiny by construction** — phone = capture + browse, never compose (D-023); the offline outbox carries only **births** (inserts, fresh IDs — cannot conflict); compose devices are online clients on one DB → exactly **three clash types** (referential · edit-vs-edit · flush name-collision). **(#7) The keep-by-default prompt IS buildable with no new stored state** — the auditor's "unbuildable" was true of the pre-hardening SPEC (hard deletes); the ruled model's **trash-is-a-freeze rows are tombstones**, so detection = a write/flush-time referential check, held on the acting device, keep-by-default = restore target + land write. **The mechanism was corrected twice before becoming law:** Claude's "one statement = atomic" was wrong (READ COMMITTED snapshot race — a statement locks nothing it merely reads; advisor's catch), and the advisor's `FOR KEY SHARE` fix was *also* wrong for this model (a **soft-delete is a non-key UPDATE** taking `FOR NO KEY UPDATE`, which key-share does not block — key-share guards existence/keys, what FK checks see, and a tombstone is exactly what FKs are blind to) → ruled mechanism = **`SELECT … FOR SHARE`** on the target in the write's transaction, **verified by a two-session race probe at translation** (I-D1). **(#6) "Last edit wins" made precise + honest:** last write to *arrive at the database*, whole-record; the false "one human in one chair" replaced with the true justification; **P12 boundary drawn** — the clash promise covers *referential* clashes only, edit-vs-edit overwrite explicitly outside (the sentence ending the contradiction); optimistic-concurrency fork rejected (cheap to detect, expensive to answer). **Standing rules:** *births-only outbox* forever + its **compose twin** (advisor's enforcement catch: an edit reaches the DB only via a live owner-present save; failed saves retry in memory, **never durably queued** — a crash-guard draft restoring into the *editor* stays legal/additive) · **born-at = the act's moment, never the sync's** (P4's offline corollary — else the return loop's time surfaces inherit day-late lies) · flush name-collisions attach via the near-dup rule. *Convergence: both mechanism corrections left the model ruling untouched — findings shrinking to the periphery as layers descend (advisor's quality-curve note).* _(owner rulings + Claude + advisor window)_
- **D-074 — CLUSTER 3 (what's a record) RULED — independent-audit findings #3/#4/#9 closed + a new finding caught (owner + Claude + advisor window, 2026-07-20 → `draft-agreements.md` P9/§2a/§2b/§2f/§6a/§7 + `lexicon.md` + `invariants.md` + `CLAUDE.md`).** **Content (#3) → owner-words-only + the computed face. ⚠ SUPERSEDES D-071 ruling #2** ("content is one field, always — auto-mirrored for text, a cached copy"), resolving the §2b-vs-§7 contradiction in §7's favor: the stored `content` holds **only owner-authored words** and the machine has **no write path to it** — "never silently overwritten" becomes *structural*, not promised. What a bit shows/searches by is the **face** (pinned in the lexicon; the doc's own phrase — §2b "its searchable face"), computed fresh: content, else per type — text: the body's plain words (no mirror; the P6 worry evaporates) · bookmark: captured title, else the URL · pdf/audio: file metadata title (genuinely mechanical — dissolves the below-the-line transcription finding) · drawing/image: the visual self. Chosen consequences named: clearing words reverts the face (a bookmark always shows something); **content locked for text bits in v1** (writable content on text *is* a title field — stays behind §2f's evidence gate; the promised "one nullable column, additive" already exists as unlocking it). **Advisor catch folded in: a bookmark's extracted title is NOT regenerable** (stored truth = the URL; the live page dies/retitles → a face navigated-by-for-a-year blanks with no owner act) → **captured once at save as machine truth**, immutable, exported — generalized as **P2's storage test applied per source** (pdf/audio rebuild from layer B → stay layer E; bookmarks can't → truth). **Category (#4) + the twin the audit missed: the subtype vocabulary (§5c "exactly like tag words") is also a stored kind** → honest count = **EIGHT record kinds, three families: things (bit · board) · acts (tag application · placement · connector) · vocabulary (tag word · category · subtype word)** — a taxonomy that retroactively names *why* D-073 was right (different families, different lifecycle rules). **P9 generalized** to all vocabulary (renames free by construction). Full "six"-sweep executed (lexicon intro/heading/table · storage map §7 · §6a "sixth record kind" · CLAUDE.md hub line · dormant table "seventh"→"ninth"); **D-log untouched — append-only history keeps its sixes.** **Bookmark (#9) CONFIRMED by the owner** — type renamed in §2a + lexicon; "link" now has zero live meanings. Free structural win: `UNIQUE (tag, target)` makes merge-dedup automatic (invariant I-R7, closing another below-the-line finding). **Parked → sharing phase:** guest visibility of vocabulary (tag/category words on shared surfaces), joining guest-pull reachability. *Meta-map recorded: Claude's failure mode = elegant-unification (watch for added hidden machinery); the doc's = elegant-absolutism (test every "always/never" at record level) — the second lens is Cluster 5's tool.* _(owner rulings + Claude + advisor window)_
- **D-073 — CLUSTER 2 (placement lifecycle) RULED — independent-audit findings #2/#5/#10 closed (owner + Claude + two advisor windows, 2026-07-20 → `draft-agreements.md` §2c/§2g/§6a/§8 + principle 12 + `invariants.md` + `lexicon.md`).** The unifying lens — *removal is a **state**, not a delete; only empty-trash destroys* — is adopted for **placements** and **boards**, but deliberately **not** for connectors (the one record kind that's genuinely different). **Placement (#10):** one **durable row per (bit, board)**; re-placing **reuses** it (first-arrival kept, presence back to here-now, last departure overwritten) — v1 stores current presence, not a visit log; "never erased" is true of the *membership row*, not of every entry/exit. **Board deletion (#5, the total gap):** trash-is-a-freeze **extended to boards** — trashing a board freezes it + its placements + connectors + tag applications, board-cards to it render absent, **the bits survive** (independent atoms); restore = exact; empty-trash cascades placements-on-it + board-cards-of-it + connectors, **never bits** (a bit orphaned of its only board → *boardless bit*, Cluster 5); a trashed board leaves pulls/find. **Connectors on un-place (#2) → KILL-WITH-CONFIRM** (chosen over *revive-on-replace*, reversing Claude's first recommendation): un-placing a card **removes** its connectors on that board, with a **proportional confirm only when any exist** (a bare card is silent) — resolving the P12 contradiction (no-nag unless something's actually lost). **Why not revive-on-replace:** an advisor pass caught that it (a) reverses §2c's own ontology (*"connectors are not remembered — arrows are live arrangement, not travel"* — connectors belong with position/size as ephemeral *arrangement*, not with placements/tags as durable *acts*), and (b) has a **zombie gap** — un-place isn't trash, so "only empty-trash destroys" never fires for an un-placed card, leaving immortal hidden connector rows with no death path; no clean GC rule exists because un-place has no terminal event. Kill-with-confirm is also the **smaller change**: it *keeps* D-071's un-place-removes-connectors and D-071 #1's trash-**hides**-connectors (revived on restore), and only adds P12's proportional-confirm nuance. **Carried to Cluster 5:** the #14 restatement (*"the pull is complete among **live, visible** things"*) and the boardless-bit home (#8). **Meta-lesson (2nd instance): the drive to unify under one elegant principle twice flattened a load-bearing distinction (connectors here; Fork C in Cluster 1) — every "and this works the same way too" now gets a second look for whether it's the same *kind* or an exception.** Refines principle 12; hardens §2c/§2g/§6a; supersedes nothing outright. _(owner ruling + Claude + advisor windows)_
- **D-072 — CLUSTER 1 (privacy & sharing) RULED — independent-audit findings #1/#11/#13 closed (owner + Claude + advisor, 2026-07-20 → `draft-agreements.md` §2a/§5/§7 + `lexicon.md`).** The cold independent audit (`agreements-review-brief.md`, run in a fresh window) caught a **silent privacy leak**: SPEC §3's anon policy exposes a *private* bit the moment it has a placement on a *public* board (the `or visibility='public'` OR), while the agreements had assigned privacy only to the bit and never defined bit×board composition (#1); boards carried no privacy field in the agreements at all (#11); and public-by-default inverts the one kind the owner calls private — morning pages (#13). **Ruled — two `visibility` fields, composed as _reachability AND visibility_:** a guest sees a bit only when its board is reachable (public) **and** the bit itself is public; **board publicity never overrides bit privacy** (principle 8 made literal). **bit** default public (ruling kept); **board** default **private**, public only by a deliberate act (a low-stakes atom leans public; a whole room is shared on purpose — a locked room can't leak). One card rule: a card shows to a guest iff its target is public; a private card renders **absent**. The safety gate becomes a **per-board publish preview** — *exactly what a guest will see* — which **replaces** §2a's old "one-time review of everything marked public" (a hand-maintained global pile = a principle-3 smell). **Fork C (contextual bit-privacy) DEFERRED**: the locked room + preview already protect private writing; re-enter only if previews prove tedious. **Parked → retrieval round:** a shared pull's reachability (live half of #14). **Translation:** SPEC §3 rewritten from this (AND, not OR; no private-default — it is stale twice, authoritative on nothing privacy-related). *Method lesson: an advisor pass caught that board-default-private + contextual-bit-privacy **compose** to reconstitute the private-by-default the owner had dropped — defaults must be judged along the real creation path, not in isolation.* _(owner rulings + Claude + advisor window)_
- **D-071 — HARDENING PASS: record-level rigor audit of the agreements → nine ambiguities/contradictions ruled (owner + Claude, 2026-07-20 → `draft-agreements.md` §1/§2b/§2c/§2g/§3b/§6a/§7 + `lexicon.md`).** A deliberate technical-rigor pass (owner: *"does this conceptually hold — a solid thing that stands on its own, a complete piece of work"*) read the agreements as *a spec a builder must implement*, tracing ordinary moments to the exact records they write. Nine findings, each ruled: **(1) Trash is a freeze, not an erase** — trashing hides a bit + its cards + its connectors; restore brings all back exactly; only a *deliberate un-place* kills a connector (resolves the trash × §6a "connector dies" contradiction against principle 10). **(2) Content is one field, always** — auto-mirrored for text (cached, not a 2nd truth), an editable *suggestion* for bookmark/pdf/audio that becomes yours once touched (resolves the storage-map "extracted title regenerable vs editable" contradiction), plain-words for rich text; **first words = the de-facto title, editable, no separate title field**. **(3) Placement history kept** — a bit remembers every board it's been on (arrived/left, never erased — erasing an act's memory is forbidden, §5); origin = first placement; any board lists all it ever held; v1 simple, not visit-by-visit. *The owner's better answer, replacing Claude's "soften the origin claim."* **(4) One category per tag; a category holds many tags.** **(5) The pull is complete _for the owner_** — a guest's pull is scoped (principle 7 had over-claimed as absolute). **(6) Board-card arrows allowed** — free; forbidding would cost a special-case. **(7) Deletion-safety = new principle 12** — destruction asks, leans toward keeping, proportional to what's lost, never nags on reversible acts (guards principle 5). **(8) Two-device clash** resolves by surfacing + keep-by-default, not silent last-write-wins. **(9)** storage map + lexicon synced. Supersedes nothing by name; **hardens D-070** — the model is now not just closed but record-level-consistent (survives translation). Remaining per §8 unchanged. _(owner rulings + Claude rigor pass)_
- **D-070 — BOARD ROUND CLOSED + remaining audit findings resolved → the conceptual model is CLOSED (owner, 2026-07-20 → `draft-agreements.md` §4/§5/§8).** Four forks ruled: **the pull is named "pull"** (D-060 closed — the philosophy's own verb); **home surface = "your boards"** (recent-first grid; doodled-home + feed deferred, §5a); **stage DROPPED from v1** (never designed; growth served by travel+pulls+revision per F3; maturity rides tags if wanted; additive later); **board-card = title + small preview**. Auto-ruled: **F7 small-card display on v1 canvas = yes** (§5b, a placement display value); **subtype vocabulary = owner-editable stored list** (§5c); **F8 PWA stays Phase 5**, logged as a deferred promise. Agreements status flipped to **model CLOSED**; §8 now lists only verification + translation (independent audit → walkthrough → SPEC rebuild + fresh migration → port). _(owner forks + Claude)_
- **D-069 — Audit round 1 verdicted; PHILOSOPHY REWRITTEN to the live model (owner, 2026-07-20 → `audit-agreements.md`).** F1+F2 confirmed → `philosophy.md` rewritten in the owner's voice: "How thoughts connect" = **shared words + shared places** (tags + boards; a direct thread "may come someday — I haven't missed it yet"); "Everything is a canvas" → **"Bits and boards"** (two kinds, matching D-018/the agreements); the welcome-list swaps fragment-links for drawn connectors; privacy line already flipped same day (public unless marked). **F3 REJECTED** — growth did not thin when links died (travel + accumulation + revision IS growth). **F4 BLESSED** — canvas serves morning pages v1. **F5 CONFIRMED** — `/export` scheduled into ROADMAP Phase 1 before real notes. **Graph confirmed achievable without links** (nodes bits/boards/tags; edges = applications + placements; local-neighborhood first). Pending: F6–F9 + the independent-window audit (`agreements-review-brief.md`). _(owner verdicts + Claude)_
- **D-068 — CONNECTORS (canvas arrows) RULED IN — owner's call, made knowingly over Claude's evidence-gate + home-not-platform flag → `draft-agreements.md` §6a.** The Obsidian-Canvas kind of arrow (arrangement), NOT the deleted meaning-link. **Anchored to placements, not bits** — the modeling choice that keeps D-067 intact: per-board by construction, dies automatically when a card leaves the board, stores no bit↔bit fact, never feeds pulls/bit-pages/graph. Sixth record kind: `connector{board, from-placement, to-placement, when}`. Behavior: anchor + live reroute (the tldraw binding model from `research-knowledge-layer.md`, found its real home), optional arrowhead, tap-delete, drag-from-edge creation, Daylight touch-tested. No labels v1 (ink annotates). **Purely additive → not in the port's critical path; lands with canvas-enrichment after port + tags.** _(owner ruling + Claude modeling)_
- **D-067 — Links RULED OUT of v1 (the word deleted from the product) · document mode explored-and-DEFERRED · v1 is CANVAS-FIRST (owner rulings 2026-07-20 → `draft-agreements.md` §6/§6b).** Ontology ruled: **no stored bit↔bit records** — bit-to-bit relatedness is always computed through a shared middle (tag or board; "introductions, no private handshakes"). Obsidian audit: 8 of 9 link-jobs already served by tags/pulls/placements/boards; the pair-tie is the lone residue; **re-entry = the first real missed moment** (a board-of-two or a single-use tag), then built small + symmetric. **Document mode** (board as ordered flow; blocks=bits at enter; calling-in rendered full or small — "a placement rendered small," record kinds unchanged at five) fully specced for later, deferred because the adversarial end-to-end trace surfaced its costs: split/merge as core edit-semantics (identity under merge!), two-truths-of-position, order+`mode` storage — all removed from v1 by canvas-first. Trace also caught: bit-birth timing, the board `mode` field, the subtype-vocabulary home (→ board round). **No title/subject split in content** — first-words is the abridged form; optional title = evidence-gated on a long bit reading wrong. Storage map completed in the doc (5 layers: DB truth · file-store truth · dormant table · account · regenerable artifacts). Canvas-only loop verified conceptually closed end-to-end. _(owner + Claude)_
- **D-066 — `draft-agreements.md` created = THE consolidated record of the working conversation (2026-07-20); `draft-techlayer.md` retired/superseded.** New rulings beyond D-065: **tags round settled** — categories emerge from use (a category = one optional label on the tag word; create/assign/dissolve free forever; no preset source/nature; kind's four words = ordinary seeded tags); picker = guided labeled chip rows; **propose-vs-apply boundary** (system proposes mechanically — context/co-occurrence/title-match/recency — only owner acts apply); pull-birth auto-tags, board-birth = **pre-lit chips offer**, never silent inheritance; **tag manager spec'd** (rename/merge/delete-with-count/category ops/dupe prevention/counts+last-use). **Links PARKED** — Claude recommends deferring them from the v1 knowledge layer (Obsidian-mapping: their link ≈ our tag; their page ≈ our pull+board; their embed ≈ our placement; only the pairwise thread has no home) with a re-entry condition (a board-of-two or a single-use tag = the petition); owner's final word pending. **Acts framing** ruled: placing/tagging/linking are acts; records = memory of acts; undo removes the record. _(owner + Claude)_
- **D-065 — The BIT aligned to the T (owner–Claude working session, 2026-07-20) → recorded in `draft-techlayer.md` (that doc is the live working surface; this entry is the pointer).** Frame: **substance / meaning / presence**, retrieval computed, never stored. Rulings: **subtype** (cartoon · script · notes · diagram — owner's vocabulary, preset chips at ✓/drop) is SUBSTANCE, its own slot — the "form" tag-category from D-062-era thinking dissolves into it; the doodle-words field is named **content** (the legend/caption naming pick dissolves — D-063 amended): auto for text, extracted where mechanical (link/pdf/audio titles), else typed/dictated; tag↔content redundancy is fine (two routes back); **links stay bare permanently** — no labels/flavors (owner: "a link is a hyperlink"); no favorites field (significance = tagging); no capture location (rejected on purpose); **no edit history v1** + **last-write-wins** cross-device (both accepted knowingly); size is per-appearance; placements timestamped → origin board + travel order derived, no origin field; **one-clock rule** (all orderings derive from birth/touch stamps). **Privacy default FLIPPED: public by default, flat** (owner ruling — supersedes philosophy's "new things start private"; philosophy.md line awaits the owner's own rewrite; per-thing one-tap toggle; nothing visible to others until the sharing phase; a review-everything-public gate precedes first publish). Storage test: **truth vs regenerable — nothing else is ever stored.** _(owner + Claude)_
- **D-064 — Every bit gets its own simple page** (not a canvas): its content + tags + boards-it's-on + links in both directions. One of *several* payoff surfaces (alongside find, the tag accumulations, and boards — the owner's correction), but the per-thought one. Needs its own plan doc **before the links build**. _(owner + Claude talk-through)_
- **D-063 — The typed-shadow rule: anything handwritten carries a few typed words so the machine can find it.** Instances: (a) a doodle's **legend** (final name pending: legend vs caption vs handle) — the schema home ALREADY EXISTS (`bits.text` = "caption otherwise", already feeding `search_tsv`); the gap is only the affordance (optional, offered at pen-Done, never forced); (b) **handwritten board titles** (owner wants these): the handwriting renders as the visual title, the typed `title` sits beneath as the searchable shadow. Also: **boards become findable** — title searchable + boards as their own result type in find (closes the "pieces as findable as fragments" red flag from the vetting ledger). _(owner + Claude)_
- **D-062 — One unified connect picker: topics first, then bits — picking a topic IS tagging; picking a bit writes a `links` row.** The owner's instinct ("wouldn't it be the same for both?") is the design: one gesture, the system files it in the right drawer; the user never chooses between "tag" and "link" mechanics. Typed triggers `#` / `[[` / `@` open this same picker (refines D-055's `[[`→link-picker-only routing). **Context supplies tags where it can:** writing inside a tag's accumulation creates a bit born tagged; a board created from inside one is born tagged; only cold-created boards need the one tap. _(owner + Claude)_
- **D-061 — Placement ≠ link; and bits may link to bits AND boards (bit→board links moved up from "later").** Placement = "lives here"; link = "points at." Being on a board is a real connection and surfaces as one everywhere (bit page "on boards", graph edges) — but never writes a duplicate `links` row: one fact, one record (an auto-link would go stale or collide with a deliberate link when the placement is removed). _(owner asked, Claude recommended, owner aligned)_
- **D-060 — Tap-a-tag = an automatic accumulation, never a curated surface ("topic page" term RETIRED; final name pending — "pull" recommended, it's the philosophy's own verb; alternatives: gathering / accumulation).** Tapping a tag shows *everything* carrying it — bits AND boards. Never hand-curated: completeness is its entire value (it must catch what the owner forgot, so it must not be able to lie). Boards stay the chosen/arranged surfaces — multi-purpose by design (creative play / consolidating a topic / ideating a piece), many-per-topic normal — and a board "about" a topic appears in that topic's accumulation via its tag. **Auto-fill boards from tags REJECTED for now** (maybe one day). **Title ≠ tag:** a title is the board's unique expressive name ("Saturn & cycles" contains no "astrology"), a tag is the shared connective thread; titles never silently become tags; when a title obviously matches an existing tag, offer it one-tap. _(owner + Claude)_
- **D-059 — The shape confirmed in the owner's own words: bits are UNNAMED; names belong to assemblies (boards) and topics (tags).** The owner's reflection: titles always felt like they belonged to boards/assemblies — which is why title-per-note tools (Obsidian) chafed; this system is Roam-shaped (unnamed fragment atoms + named connective concepts), and that is now confirmed intent, not accident. **Display-label convention** wherever an unnamed bit must be shown (search results, pickers, backlink sections, graph nodes): its first words; doodles/images show their legend. No forced titles anywhere, ever. _(owner + Claude talk-through)_
- **D-058 — Vetting process adopted → `draft-vetting-technical-layer.md` (owner named it: filename = the activity + the thing; the owner's step-back: "did we even plan the right *set* of things? will it work when it plays out?").** Core: **three levels of proof** (L1 mechanically valid / L2 faithful translation / L3 works-as-a-tool per the D-053 return test) — every "done" must name its level; **completeness before correctness** — coverage checked from two directions (top-down *promise audit* of `philosophy.md` → every promise maps to a planned piece or is flagged UNMAPPED; bottom-up *scenario walkthrough* → 6–8 real moments traced through exact rows, verdict fits/awkward/missing — scenarios drawn from the owner's life, never from the plans); **the loop** — audit → walkthrough → owner-as-oracle review → fix the model *first* (migration before the port) → port + seed real content → rung-by-rung with use-gates; **a live evidence ledger** in the doc (honest current state: schema L1-only with known strokes/fontSize gaps; transclusion live-sync — the differentiator — entirely unexercised; knowledge layer all paper). Two coverage red flags already visible for the audit to confirm: owner-facing **export** ("I own all of it") has only a *backup* planned, and **boards aren't in search** ("pieces as findable as fragments"). Walkthrough is inserted **before the port**. _(owner step-back + Claude)_
- **D-057 — `SPEC.md` reconciled to the live migration (docs-drift caught + fixed).** The owner flagged that `draft-datamodel.md` and `SPEC.md` might be "based off the same/right docs" — they weren't. Verified `draft-datamodel.md` against `supabase/migrations/20260715000001_init.sql` (matches); found `SPEC.md` had drifted: old `canvas` naming in prose, a **phantom `rotation` column** on placements (dropped in D-028, never in the applied schema), a wrong "`kind`/`stage` not in migration" claim (`kind` *is* in; only `stage` isn't), and missing `kind`/`deleted_at`/`search_tsv` on the bits schema. Fixes: `canvas`→`board` sweep (protecting the legit *canvas-mode/view/scale* phrases); added a naming note; corrected the schema lines; added `perfect-freehand` to the stack; and marked §12's capture-first build sequence **superseded by `ROADMAP.md`** (D-035). Re-grepped clean; all three docs (datamodel · SPEC · migration) now agree. Reinforces the standing "docs must never drift" rule (an external review caught us on exactly this once). _(owner instinct + Claude)_
- **D-056 — Privacy built *gradient-ready from day one* (owner: "set it up properly at first, not correct it later").** Clarified the split: **login = the *author* key** (only the owner writes; needed for any cloud save under RLS) — NOT a wall in front of readers. **Viewing** is the private/shared/public gradient (the `visibility` enum already exists on `boards` + `bits` from the init migration). So the owner login is the **permanent foundation, not throwaway** — the gradient is purely *additive* (anon-read-public RLS policies + a per-board visibility toggle + public viewing routes), zero rework of the foundation. **Concrete guardrail:** build Phase-1 auth/routing **public-capable from the start** (routes may be public for public boards; do NOT hard-wire whole-site login) so the public diary slots in cleanly. The gradient *feature* still lands after the cloud port (nothing to publish before then), but nothing built now needs undoing. _(owner + Claude)_
- **D-055 — Typed link/tag triggers are now WELCOME (softens the "no `[[ ]]`" constraint the research was scoped to).** Owner: "I'm happy to use `[[ ]]` too." Resolution: **tap/drag stays primary** (it's what works on the Daylight and matches the no-syntax spirit), and typed triggers become a keyboard *bonus path* — `#` in Tiptap opens the same tag picker inline; `[[` / `@` will open the link picker. Strictly additive to the D-054 design, no rework. Tags planned in detail → **`draft-plan-tags.md`** (schema deltas incl. join-table `created_at` + case-insensitive tag names; `lib/db/tags.ts` API; the shared `<TagPicker>`; topic-page; tag manager; build steps A–G). Build is **gated on Phase 1 cloud**. _(owner + Claude)_
- **D-054 — Knowledge-layer research done → `research-knowledge-layer.md`** (cited; the Phase-2 design input). Actionable calls: **tags stay flat** (Capacities' type/tag/collection split ≈ our `kind`/`stage` vs `tags`; don't nest until a merge/rename tool proves it needed). **Linking without `[[]]` = two gestures to the same `links` row:** (a) a tap/`@` **search-picker** (Heptabase/Notion) everywhere; (b) a **bespoke SVG drag-to-connect overlay** on `react-rnd` positions in canvas mode — copy tldraw's *binding model* (anchor / precise / reroute-on-move), **NOT** the tldraw library (not in the approved stack). **Topic-pages = a query-time view** over the existing join tables (no new table). **Graph = local-neighborhood first** (global graphs turn to hairball past ~200 nodes) via **`react-force-graph`** (best React fit; needs dep approval). **Growth mechanic = no new feature** — the planned backlinks panel on the bit detail page + `stage` maturity IS the visible lineage (Matuschak "revise in place"). _(research + Claude)_
- **D-053 — Roadmap REPRIORITIZED around the owner's north star ("Obsidian + an interactive canvas") after a fresh-eyes plan review.** New canonical sequence (in `ROADMAP.md`): **cloud+login+backup → the knowledge layer (tags · backlinks · forward-links · find · topic-pages — built beautifully + *with* real content) → graph view → richer boards + growth mechanic → (later) phone capture → browse/resurface feed → privacy gradient**. Adopted from the review: **login wall is Phase-1 non-negotiable** (don't put real notes on the public deploy); **"grow" must be a real *feature*** (the connect/tag/graph layer *is* it — topic-page as a *living surface* + link lineage); **build the knowledge layer *with* seeded content, not before it**; **success metric = "do I open it unprompted after ~3 weeks to connect/develop?"** (not "does compose feel good"). Owner's own calls: knowledge layer = **highest value, do it beautifully** (research pass greenlit); **capture + feed = later** (matter with volume); **migration = manual** (Apple Notes won't auto-export cleanly — no importer built); **rotation deprioritized**. _(owner + fresh-eyes review + Claude)_
- **D-052 — ✅ PEN CONFIRMED WORKING ON THE DAYLIGHT (real stylus).** After the canvas fix (D-051): draw → lift (no crash) → Done → resize → move all work; owner: "everything basically looks perfect." The whole handwriting vision is validated on-device. **Aside — the `ERR_CERT_COMMON_NAME_INVALID` scare was NOT the app/deploy:** the Daylight's browser was dropping **SNI**, so Vercel served its `no-sni.vercel-infra.com` fallback cert (name ≠ `world-website.vercel.app`). Diagnosed by comparing the served cert with vs without SNI. **Fixed by fully closing + reopening the browser** (stuck connection state). Filed: if it recurs, restart the browser. _(owner Daylight test + Claude)_
- **D-051 — Second Daylight pen bug: crash-on-pen-lift was the RENDERING METHOD, not the palm (that fix held).** Owner confirmed: palm no longer marks + stylus draws, but lifting still crashed. Root cause: the draw overlay rebuilt the *entire drawing as SVG DOM on every pointer sample* — dozens/sec — and the tablet's lighter browser couldn't sustain the layout/allocation churn; the renderer crashed as the stroke peaked (desktop's power masked it). Decisive clue: the validated pen **spike drew on a `<canvas>`** and ran fine on the *same* Daylight → the variable is canvas vs regenerate-SVG-per-frame. **Fix (`draw-overlay.tsx`): live drawing now on a `<canvas>`** (rAF-throttled, `Path2D` fill — cheap, matches the spike); strokes still kept as vectors; the finished doodle bit is still crisp SVG (rendered once). Verified end-to-end on desktop (canvas draws → Done → doodle bit); redeployed. Owner re-testing on Daylight. _(owner Daylight test + Claude)_
- **D-050 — First Daylight pen test surfaced two bugs — found, fixed, redeployed.** (1) Resting a palm on the screen drew marks. (2) Lifting the pen **crashed the tablet browser** ("this page could not load"), losing the un-finalized drawing. Root cause (one, not two): palm + stylus fire as **competing simultaneous pointers**, and the tablet choked on the tangle (desktop never hit it — a mouse is one clean pointer). Fix in `draw-overlay.tsx`: draw only from **pen/mouse** (ignore `pointerType === "touch"` = palm/finger), **one active pointer at a time**, plus a non-finite-coordinate guard and `pointercancel` handling. Typecheck clean, redeployed. Safe for the stylus because it reports `"pen"` (the spike proved it gives pressure). Finger-draw stays a deliberate "later." _(owner Daylight test + Claude)_
- **D-049 — Compose prototype DEPLOYED to Vercel — LIVE + public: https://world-website.vercel.app** (homepage `/` → `/compose` via a proxy redirect added for the prototype). Deployed with `vercel --prod` (owner authed as `siva-sakti`, scope `gargis-projects`). **No cloud DB wired** — the board is localStorage, **per-device** (no cross-device sync yet; that's the DB step). **No auth/privacy layer yet** — reachable by anyone with the URL; owner OK for now (nothing sensitive on it), add the private/public lock before that changes. Prod build green (`○ /compose` prerendered static). **Custom domain pending** (owner's, via Vercel dashboard → Domains + DNS). **Unblocks the big one: on-device Daylight testing of the whole compose surface — especially the pen with a real stylus.** _(owner + Claude)_
- **D-048 — Infinite canvas = a LATER "camera" layer; today's coordinate math is forward-compatible (owner asked if it breaks the pixel math — it doesn't).** Infinite pan/zoom will be a CSS `transform: translate() scale()` on a container holding all the bits (the tldraw/Freeform approach from `draft-research-canvas.md`). Bits already live in a **screen-independent coordinate space**, and doodle strokes are stored **relative to their own bit** — so the camera never touches bit/stroke math; a doodle just moves/scales/zooms with its bit. The *only* change when we add it: the client→space conversion at capture points (new-note position, image-drop position, pen stroke points) gains a pan offset + zoom divisor — I'll centralize those into one `screenToWorld()` helper. `react-rnd` `scale` prop wired to zoom (D-030). **Sequencing:** add the camera *after* the bit types + interactions are solid on the fixed board (nearly there) — it drops in as a wrapper, no rewrite, nothing built now gets undone. _(owner raised + Claude)_
- **D-047 — Text corner-scale BUILT (v1), owner chose the Freeform "drag a corner" over an A−/A+ control (D-045's open question, resolved).** A distinct **filled-blue handle at the bottom-right** of a text note; dragging it scales width + font size by one factor (top-left anchored, custom pointer handler — *not* react-rnd's corner, so no auto-height fight), so the note zooms while the wrap stays put. Padding switched to `em` so it scales too. `fontSize` stored on the bit. **v1 = one corner (bottom-right)**; extend to all four corners if the feel lands. Render + typecheck verified; **drag-feel is owner's to confirm** (didn't self-test by dragging, to avoid mutating the owner's live, un-backed-up board). _(owner + Claude)_
- **D-046 — Rich-text vision (owner): the text bit should grow into a full rich-text editor** — headings, bold/italic/underline, lists, tables, inline media, links, maybe multiple fonts. **Feasible — already on Tiptap:** StarterKit gives bold/italic/headings/lists/quote/strikethrough today (even via ⌘B), and underline/links/tables/inline-image are official add-on extensions. Needs a **formatting UI** (selection *bubble menu* + toolbar) → folds into Task 4. **Tension to flag:** multiple font *families* fights the "one typeface, quiet" stance (D-008) — bold/headings/italic are expressive within one face; distinct fonts are a deliberate departure = owner's explicit call. Inline media that text flows *around* = the D-044 wrap dream (later). _(owner + Claude)_
- **D-045 — Text resize = two gestures (Freeform model); the reflow half is BUILT + verified, refining D-040.** Owner named the confusion exactly: *"reshape the box"* vs *"scale the whole thing"* are different operations. Split by handle: **side handles (left/right) = reflow** (change wrap width → text re-wraps; height follows the text, so there are **no top/bottom/corner handles** — which removes the earlier jumpy snap); **corner handles = scale** (images already do this, aspect-locked; text = TBD). Reflow verified in-browser (side-only dots render; `tsc` clean) without disturbing the owner's live board. **Open — owner's choice: how to scale *text* — drag-a-corner-to-scale-the-font (Freeform feel) vs a size control (A−/A+, always crisp & precise).** Supersedes D-040's "every handle reflows" (that was the buggy version). _(owner + Claude)_
- **D-044 — Text-flow *dream vision* recorded (parked / someday).** Owner's north star: text should eventually **flow around arbitrary forms/shapes** (magazine / Freeform-style inline wrap), not be confined to its own rectangle, with elements able to sit under/around it. This is the existing **"wrap-box"** later item; the **MVP text bit stays a simple rectangular reflow box**. Not near-term — captured so it isn't lost. _(owner)_
- **D-043 — Image bit built (compose Task 2, minus crop).** Drop an image onto the board, or "+ image" (file picker) → the file is **downscaled** on import (canvas → JPEG, ≤1400px longest edge, q0.82 — keeps localStorage sane) → placed as an **aspect-locked** image bit at the cursor (default ~320px display width). Renders filling the card (no paper padding, clipped to the rounded frame); resize preserves aspect (`react-rnd` `lockAspectRatio`). Non-image files and read/decode failures are ignored (never throw into the drop handler). Verified end-to-end via a real simulated drop + typecheck. **Crop deferred:** non-destructive crop (D-016) likely needs a new dep or a hand-built cropper — owner's call, so not built. _(Claude)_
- **D-042 — Visible resize handles: subtle dots on the selection frame's four corners** (`react-rnd` `resizeHandleStyles`), shown only when a card is selected-not-editing. Makes the owner's "easy to see the edges so you can resize" literal, and gives touch (Daylight) a real grab target vs. invisible edges. Verified in-browser. _(owner feedback + Claude)_
- **D-041 — Card interaction = click-to-select-then-edit two-step; root-caused a real bug, fixed at the cause not the symptom.** Symptom: a *single* click jumped straight into text-edit, which also hid the resize frame (frame only shows when `selected && !editing`). Root cause (found by reading the code, not guessing): react-rnd's `onDragStart={onSelect}` fires on pointer-*down*, pre-selecting the card, so the pointer-*up* click saw `selected===true` and chose edit. Fix: select on the inner div's pointer-down and gate edit on *"was it already selected before this click"* via a ref; removed `onDragStart`. New notes still open in edit mode (creation sets `editingId`). Verified: click→select (frame, no caret), click→edit (caret). _(Claude; owner-directed "diagnose where it is, then build — don't patch and create debt")_

### Review-driven (external evaluation, then reconciled with owner)

- **D-017 — Build sequence inverted to CAPTURE-FIRST.** Foundation → cloud deploy → capture loop → browse+resurface → data-safety → tagging → compose. The daily loop (capture + return) ships before the sit-down compose tool. _(review F1 + owner)_
- **D-018 — Ontology: the fragment is the atom; placement-optional.** Capture creates a *bare* fragment (no board, no placement); a fragment can live on zero/one/many boards. Supersedes D-001. SPEC §0/§5 to be rewritten to match. _(owner)_
- **D-019 — Boards are two-mode:** *collection* (grouped fragments, no spatial layout — quick, any device) → *canvas* (same fragments given positions, arranged spatially — sit-down). Position on a placement is optional. _(owner)_
- **D-020 — Two tag roles:** **topical tags** = open, growable, *linkable topic-nodes* (each a page collecting everything about it = Obsidian-style backlinks, tap-not-type UX) — these are the connective/graph web. **Categorization** = `kind` (fragments) + `stage` (boards), fixed enums. _(owner)_
- **D-021 — Data safety:** soft-delete/trash for fragments; automated export/backup from day-one-of-real-data; export belongs *in* the build queue (was a spec contradiction). _(review F4 + owner)_
- **D-022 — Tagging at capture = optional + fast, never required to save.** Refine while tending; images may be tagged at capture or later. _(owner + review F7/F8)_
- **D-023 — Daylight is a first-class compose device** (touch, **landscape-only**); phone = capture + browse only. **Rotation cut from v1** (no approved lib provides it). **Graph view demoted** to later/scoped (backlinks deliver the value; the topical web is the graph). _(owner + review F6/F9)_
- **D-024 — `kind` is optional, no default, set at tending;** build no filters on it until real data shows it's assigned. _(review F8 + owner)_
### Feasibility-review-driven (second external review: "will it build / will it be the thing")

- **D-025 — Capture transport:** media uploads go **client → Supabase Storage directly**, never through a Vercel function (4.5 MB serverless body cap vs 3–8 MB iPhone photos; server actions cap at 1 MB). App-wide rule. _(review A1)_
- **D-026 — iOS capture auth = bearer capture-token** (`CAPTURE_TOKEN`) validated by `/api/capture`, which uses the service-role client (narrow, deliberate RLS bypass). Shortcut resizes or exchanges for a signed upload URL — decided in the spike. _(review A1)_
- **D-027 — Offline outbox, honest guarantee:** no Background Sync on iOS → queued captures flush **on next open**; visible "N waiting to sync"; capture shell is a precached client route with a ≤ ~1.5s tap→keyboard budget. Service worker via `serwist` — **new dep, owner approval at phase 2 start**. _(review A2/B1)_
- **D-028 — Init migration amended in place** (nullable x/y + check for collection mode, `kind`, `deleted_at`, HTML-stripped tsvector, rotation column dropped): legitimate because it had only ever been applied to throwaway validation containers — no persistent DB exists. Re-validated against Postgres (unplaced row stored; half-placed row rejected) + clean build. **`stage` deliberately deferred to the boards-phase migration** — its shape (steps/names) is still the owner's open question and boards hold no data until then. _(review A3, nuanced)_
- **D-029 — Backup = GitHub Actions nightly cron** (`pg_dump` + storage sync → private artifact/R2), doubling as the **keep-alive** against free-tier auto-pause. _(review A4)_
- **D-030 — One drag owner per gesture:** react-rnd owns on-canvas drag+resize; dnd-kit owns tray/grid→board pulls; react-rnd `scale` prop wired to canvas scale. _(review A5)_
- **D-031 — Image encode: JPEG on iOS** (Safari can't encode WebP), WebP elsewhere; EXIF orientation honored; buckets + pipeline land in **phase 2** (photo capture drags them in). _(review A6)_
- **D-032 — Both spikes run at phase-2 start:** the one-day end-to-end iOS capture spike (the single biggest risk) and the Daylight pen-feel spike (its outcome reshapes post-compose plans). _(review §4 + B2)_
- **D-033 — Phase-3 grid: the *text* preview card is the primary designed object** (early corpus is mostly text); phones render boards in **collection mode**; phase 5 adds an "untagged recent" strip on home. _(review B3/B4/B5)_
- **Schema gap (obsolete framing):** the whole schema is being **derived fresh from `draft-agreements.md`** in the translation step (empty DB → one clean migration). `stage` is dropped (D-070). Ignore the old per-column gap list.

## Needs owner (blocked on you; not touched while you're away)

*(Rewritten 2026-07-27, D-106 — the previous list was frozen at the 07-21 state; that history lives in the D-log.)*

- **✅ Done since this list was written:** the D-106 fix pass shipped; D-107 (de-hardcode → per-row ownership) + D-108 (the public read-door) were **applied to the live database** on your "go" (fresh backup first · atomic · verified — 0 data lost, your access intact, nothing public yet).
- **▶ Add the backup secret** — `SUPABASE_DB_URL` in GitHub → Settings → Secrets → Actions (steps in `.github/workflows/nightly-backup.yml`), then hit **Run workflow** once → nightly backups go live. The workflow is on `main` now; it just needs the secret.
- **▶ Feel-test the live app** — log in and confirm your notebook is normal after the migration (boards load, add a note, tags/source work). The security-rule change is invisible if correct, but you're the only one who can log in as you.
- **Storage-bucket RLS — a 2-min dashboard eyeball** (Supabase → Storage → Policies): confirm only your role reads/writes the `private` bucket. Unverifiable from the repo.
- **Storage-bucket RLS — verify in the Supabase dashboard** (review, D-106): the `private` bucket's object policies are dashboard config, unverifiable from the repo. Two minutes: Storage → policies → confirm only the owner role reads/writes. I can walk you through it.
- **Feel-tests owed:** call-in's return trip (place → remove → place again — same spot logic, no error) · the intake reset + tags-at-capture · the source manager's edit/merge/delete (all hit your DB; I can't log in as you).
- **Decisions queued (each small, none urgent):** the §2h liveness sweep beyond call-in (stale-device writes on trashed things — the ruled FOR SHARE mechanism) · file-size-ceiling refactors (board-surface 489 · bits.ts 268 · three new ~215-line files — flagged, not refactored solo) · source URL-conflict surfacing (parked A23) · image bring-in paint-order polish · the two rare banner nudges (manager status · board save-error) · workspace source-field link-paste parity · **empty-the-trash** (the one irreversible act — wants its own planned session, say when).
- **Any dependency beyond the approved list** — deferred and listed here rather than installed.

## Open questions (parked, not urgent)

- Mobile: how much spatial arranging, if any, on the phone (default: capture-linear on phone, arrange on desktop).
- Revisiting at scale: does the feed suffice or do tags/graph carry it (owner: don't worry about it for now).
- v1 cut line vs the full queue above.

## Work done

**Pen (Task 3) — DONE, owner-verified on desktop ("works great"); Daylight stylus/pressure test pending deploy:**
- `perfect-freehand`→SVG (pre-approved dep, the Daylight-validated feel). `✎ pen` → a full-board draw overlay captures strokes live as a filled-outline ink (`#1c1813` on paper) → **✓ done / ✕ cancel**. Modular: `stroke.ts` (path + bounds), `draw-overlay.tsx`. Typechecks clean.
- **Model (owner-confirmed):** the pen-session between tapping the pen and tapping **Done** = ONE doodle bit. No per-pen-lift, no proximity-grouping (too complex) — just Done finalizes. A finished doodle is a normal bit (drag/resize/select like text/image). Different pen types/colors = later.
- **Step 2 DONE + owner-verified on desktop:** ✓ Done bundles the session's strokes into ONE movable doodle bit — `finishDoodle` computes the bbox, stores strokes relative to the bit; `doodle-bit.tsx` renders SVG with a native-size viewBox so the ink scales crisp; transparent card (just floating ink), aspect-locked corner resize; persists on reload. Owner: "works great."
- **Daylight (touch + real stylus pressure) NOT yet tested — deploy-gated** (LAN dev doesn't hydrate on the Daylight). The pen engine is the same `perfect-freehand` (pressure-aware, `simulatePressure:false`) that was validated on the Daylight in the phase-2 spike, so confidence is high — but it needs a real hosted URL to confirm on-device.
- **⚠️ Turbopack dev gotcha (cost ~an hour):** new `globals.css` rules can compile clean yet never reach the browser CSSOM — computed styles show defaults despite the rules being on disk. It's **not** a selector/specificity bug; Turbopack served a stale CSS chunk. **Fix: restart dev server + `rm -rf .next`.** Verified via CSSOM inspection, not by guessing.

**Compose MVP prototype (`/compose`, localStorage) — Tasks 1–2 done + verified in-browser:**
- **Task 1 — board + universal card + text bit.** `/compose` client route (no cloud; the auth proxy early-returns for `/compose` so it loads with no DB). One universal draggable/resizable card (`react-rnd`) reused by every bit type; text content = Tiptap. Create via "+ note" or double-tap empty. Text box opens at a general width (240px), **wraps + grows downward**; drag an edge to change width, text re-wraps (D-040 — dropped the old single-line "hug"). **Click selects** (frame + 4 corner handles, D-042); **click-again edits** (D-041 — fixed a real bug where a single click skipped to edit and hid the resize frame). Persists across reload.
- **Task 2 — image bit (minus crop).** Drop onto the board / "+ image" → downscaled, aspect-locked image card (D-043).
- **Verified in-browser** (localhost, Chrome): create/edit/hug/persist, the select→edit two-step, side-handle reflow (D-045). `tsc --noEmit` clean. **Image bit confirmed by the owner** dragging in real JPG/PNG photos (add/resize/drag, live retreat board).
- *Honest correction:* my earlier "image verified end-to-end" was only a **code-generated PNG via a synthetic drop, not a real file**. The real file-picker path fails **silently on HEIC** (Chrome can't decode Apple's HEIC — patent-locked HEVC; Safari can) → now a to-do (surface a message; conversion later).
- **Gaps for next:** no delete-a-bit yet; unreadable-image message (no silent fail) + HEIC support; text *scale* gesture (D-045, owner's choice); crop (dep decision); Daylight touch is deploy-gated (LAN dev doesn't hydrate there).

**Research + spike pass (compose-first prep) — done:**
- `draft-research-canvas.md` — studied JSON Canvas / tldraw / Heptabase / Excalidraw. Conclusions: board = **DOM cards in a CSS-transformed container** (tldraw's approach, gets Freeform-smoothness + rich text); keep `bits`+`placements` (richer than JSON Canvas — it has no shared-card-across-canvases); **JSON Canvas's `group` node resolves the open "is-everything-a-bit?" question → allow a few non-bit structural elements**; edges→links; perfect-freehand→SVG pen (vector, tiny). Optional JSON Canvas export for interop.
- **Pen-feel spike → tested on the Daylight: WORKS.** Owner: "a great basic pen, does the trick, working perfectly." **→ in-app web-pen handwriting is a VIABLE path (D-037 validated) — we build handwriting into the app; Freeform not needed for it.** Bugs found + fixed during testing: mobile disappear-on-lift (→ strokes-as-vector-data, redraw, dropped `desynchronized`), weird stroke-start cap, and black-ink-invisible-on-dark (→ theme-aware ink). https://claude.ai/code/artifact/406f1d0f-0d9d-4d6e-a5e1-3c23d36b48e5 _(Learnings for the real pen: perfect-freehand→SVG vector = tiny + robust; store strokes as data not canvas pixels; theme-aware ink.)_

**Foundation (queue item 1) — done; builds + typechecks clean:**
- Next.js 16 + React 19 + TypeScript strict + Tailwind 4, scaffolded into the repo.
- `supabase/migrations/20260715000001_init.sql` — full schema (canvases, bits, placements, tags, bit_tags, canvas_tags, links) + `updated_at` triggers + FTS tsvector + indexes + RLS (owner-all; anon default-denied). **Validated by applying it to a throwaway `postgres:16-alpine` container — clean.**
- `src/lib/supabase/{client,server,admin}.ts` — browser / server / service-role clients.
- `src/proxy.ts` + `src/lib/supabase/proxy.ts` — auth session refresh + route guard (Next 16 proxy convention).
- `src/lib/types.ts`, `src/lib/storage.ts` (single storage access point), `src/lib/db/{canvases,bits}.ts`.
- Auth: `/login` (Supabase Auth email+password) + sign-out.
- Quiet base layout + globals (white, one typeface).
- `/` — list + create canvases, sign out, empty state.
- `/c/[id]` — canvas view rendering placed bits + add-text.
- `scripts/create-owner.mjs` — idempotent single-owner setup. `.env.example` + `.env.local`.

**Canvas (queue item 2) — started:** static render + add-text works. Drag / resize / rotate (dnd-kit + react-rnd), undo, and viewport scaling are **intentionally deferred until the DB is running**, so they're verified rather than guessed (no-debt).
