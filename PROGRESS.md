# PROGRESS

Running log of the build. Read every session (after `draft-claudemd.md`); update as work happens. Newest notes on top within each section.

_Last updated: 2026-07-16_

## Status

> **📍 What's next lives in `ROADMAP.md`** (the canonical, reordered sequence — D-053). Read that first for *what to build*; read this section for *where we are right now*.

**✅ Compose board MVP: DONE, deployed, and validated on the Daylight with a real stylus** (text reshape+scale, image drop/resize, handwriting/pen — all as movable bits). Live at **world-website.vercel.app**. It's a **localStorage prototype** (per-device, no cloud, no login, currently **public** — no real/sensitive notes on it until the login wall ships in Phase 1).

**Plan reordered (D-053)** around the owner's north star — *"Obsidian, but an interactive canvas."* Sequence: **1) cloud + login + backup → 2) knowledge layer (tags/backlinks/forward-links/find/topic-pages — do it beautifully) → 3) graph → 4) richer boards + growth mechanic → 5) phone capture → 6) browse feed → 7) privacy gradient.** Success metric = *"do I open it unprompted after ~3 weeks to connect/develop ideas?"*

**Right now:**
- **Next build = Phase 1 (cloud + login + backup)** — *blocked on owner accounts* (cloud Supabase keys + Vercel + domain).
- ✅ **Phase-2 knowledge-layer research DONE** → `research-knowledge-layer.md` (cited web research; the design input for the knowledge layer). Key calls captured in **D-054**.
- The compose-polish tasks (delete-a-bit, crop, HEIC, rich text, rotation) are **deferred to "later/treats"** per D-053 — real, but not next.

## Build queue

> **Canonical sequence now lives in `ROADMAP.md`** (compose-first per D-035). The capture-first list below is kept for history — it's the *original* ordering before the D-035 reprioritization; follow ROADMAP for what's next.

### (historical) CAPTURE-FIRST ordering — the daily loop before the sit-down pleasure; reordered per the external review, F1

1. **Foundation** — scaffolded locally (done). Remaining: **cloud Supabase + deploy** (owner accounts) so a phone can reach it. *Critical path.*
2. **Capture loop** — deployed PWA; capture a **bare fragment** (text + photo), optional quick tags; iOS **Shortcut → capture endpoint** (Web Share Target absent on iOS Safari); **offline outbox**. Real data starts landing.
3. **Browse + resurface** — a *designed* fragment grid (image-forward; presentation is an explicit exception to "no aesthetic decisions") + a few **random old fragments on home**.
4. **Fragment detail + edit + soft-delete (trash) + backlinks + automated export/backup** — data safety from day one of real data.
5. **Tagging** — tap-existing **+ create-new**; topical tags = linkable topic-pages; `kind` optional (set at tending); tag-filter on the grid.
6. **Pen-feel spike on the Daylight** (throwaway) → **Boards** — collection mode (group fragments, any device) → canvas mode (model-B spatial: drag/resize, **no rotation v1**, text/image/doodle, touch + landscape).
7. **Stage on boards + a boards-by-stage / tending view.**
8. **Privacy tiers** — private / shared-key / public.
9. **Later / maybe** — scoped graph view; in-app calligraphic pen (if spike passes); audio/pdf bits; wrap-box.

## Decisions log

- **D-001 — One primitive: the canvas. → SUPERSEDED by D-018.** (Later corrected to: the *fragment* is the atom; a fragment needs no board.) _(owner)_
- **D-002 — Auth: Supabase Auth, single user.** Browser gets a real authenticated identity so RLS "to authenticated" works verbatim and future privacy tiers slot in; avoids the shared-cookie approach that breaks RLS. _(recommended default, unobjected)_
- **D-003 — Bits are the atom.** A bit (text/image/doodle/audio/link/PDF) is the taggable, dated, retrievable unit; per-bit tagging in v1. Canvas is also taggable, but a tag-pull surfaces bits. _(owner)_
- **D-004 — No "attributes." → SUPERSEDED by D-013.** (Originally dropped stage/kind; later reinstated, placed differently.) _(owner)_
- **D-005 — Dates:** created + edited on every bit and canvas, auto-maintained by a DB trigger. _(owner)_
- **D-006 — Spatial is core**, not a "later/play" layer. Free-canvas drag/place; desktop-primary editing accepted; capture-simple-then-arrange. _(owner)_
- **D-007 — Doodles:** import PNG now; light in-app finger-draw later; never a full drawing app. **Audio:** attach files now. _(owner)_
- **D-008 — Design:** quiet/white/fast chrome, no aesthetic decisions made for the owner; expression from owner's content; layout inspiration = indie-web "vibes" pages. _(owner)_
- **D-009 — Stack:** Next.js + Supabase (local via Docker) + Tailwind + pre-approved libs. Local-first dev; cloud wired later by the owner. _(recommended default, unobjected)_
- **D-010 — Delivery:** installable PWA (phone + desktop). _(owner)_
- **D-011 — Local Supabase not started, full image stack not pulled.** Disk at 97% (~15 GB free); pulling ~6–8 GB while the owner is out risks their machine. Everything else built + verified offline (schema validated on a throwaway Postgres container). _(Claude, no-debt / no-risk call)_
- **D-012 — v1 RLS is owner-only; anon default-denied.** No anon read-public policies yet (nothing is public); they arrive with the privacy-tiers phase in their own migration. _(Claude, matches build sequence)_
- **D-013 — Attributes reinstated, and *placed*:** `kind` (learned/noticed/wondered/theorized) on **fragments**; `stage` (ordered, ~3–4) on **boards**. Both distinct from open **topical** tags (aboutness, many). Supersedes D-004. _(owner)_
- **D-014 — Assemble-a-page = model B:** manually-placed discrete boxes (text/image/doodle), drag/resize/rotate; each text block wraps in its own box. A wrap-around-a-doodle box (model A, **pretext**) is a later, occasional type — not a v1 dependency. _(owner + Claude)_
- **D-015 — Single-user now; multi-user/product a possible later, not designed out.** Supabase Auth + RLS is already the multi-user shape; don't over-build for it now. Softens philosophy's "never a product." _(owner)_
- **D-016 — Media + doodles:** import auto-sizes (aspect-locked) + in-place non-destructive crop; **transparency, not crop, makes doodles look nice** (transparent PNG, else knock out white on import). Light in-app calligraphic pen (**perfect-freehand**) post-v1; a native app is contested only if the web pen genuinely fails. _(owner + Claude)_
- **D-034 — Naming DECIDED: `bit` (kept) + `board` (renamed from `canvas`).** Code + migration renamed and re-validated (build clean; migration applies). The spatial *mode* of a board stays "canvas mode." SPEC prose reconciles canvas→board per-section as we build. _(owner)_
- **D-035 — Near-term REPRIORITIZATION: build the board/compose (topic-home) MVP first**, not capture. Rationale: it's what the owner's mind wants first, it's the soul/differentiator, and it validates the whole loop with real content (the retreat notes). **The capture-flow work is NOT lost** — fully designed + recorded (SPEC §4b, D-025–D-033), deferred until wanted. Fragments still exist (atoms of boards) and can be **created right on the board** or brought in. The two-review capture-first phasing stays as the *eventual* plan. Insight behind this: capture (scattered mode) and compose (focused mode) are two real modes served by two layers — fragment + board — that are one system; we over-focused on capture *plumbing*, not the fragment layer itself. _(owner)_
- **Open question (think more):** is *everything* placed on a board a first-class, findable fragment, or can a board hold some **local/structural** bits (a title, a label) not tracked in the fragment pool? Doesn't block the MVP — build the simple version (things made on a board are bits), refine by feel.
- **D-036 — Freeform comparison VALIDATED the model, and clarified the differentiator.** The owner wants a compose-canvas (Freeform-like) **plus** a knowledge layer where **each element on a board is a first-class, taggable, referenceable bit** pulled from a shared heap (a bit can sit on one *or more* boards, live). Freeform (no bits/tags) + Obsidian (no spatial integration) *glued together* still can't give this — the value is the **integration**, which nothing off-the-shelf does → **building is justified.** _(owner + Claude)_
- **D-039 — Tagging is always optional, never forced — and not everything on a board is a findable entity.** Owner: idle doodles, a title, a corner scribble don't need tags; a real chunk of notes does. Resolution: **you indicate significance by tagging** (no separate "is-this-a-bit" toggle needed for the MVP) — untagged marks just live on their board; the *find/heap* surface defaults to what you've **tagged/kept**, so idle marks don't clutter it. (Matches JSON Canvas's `group`/structural insight.) The tag UI is the **Obsidian layer** — it comes *after* the compose MVP, so nothing is forced in the MVP by construction. _(owner)_
- **D-038 — Freeform observation (owner's research half): text auto-forms a content-sized bounding box that becomes a movable object.** = exactly our **bit as a movable card**: creating text/handwriting makes a bit whose card **auto-sizes to its content**, then is draggable/resizable. Not a "later" step — it *is* the core board interaction. Target UX: create → a card forms around it → move/resize. _(owner + Claude)_
- **D-037 — Handwriting is IN-APP (corrects D-036's "draw in Freeform, import").** Freeform's pen is **iPad-only** (no Mac desktop) and Freeform is **Apple-only** (won't run on the Android Daylight), so it **can't be the cross-device drawing surface**. So in-app handwriting via **perfect-freehand** is the *primary* path — it's the only thing that runs on the Daylight. Validated by the phase-2 **pen spike on the actual Daylight** (that spike now tests our *real* handwriting solution, not a fallback). Import-a-doodle stays available for anything drawn elsewhere. The Daylight is a fast (60Hz) display, not slow e-ink, so web-pen latency should be workable — the spike confirms. **Freeform stays a *feel-benchmark* only, not a device we can rely on.** _(owner + Claude)_
- **D-040 — Text-box default sizing (corrects the first cut of D-038): a text bit opens at a fixed general width (240px), wraps, and auto-grows *height*; dragging an edge changes width and the text re-wraps.** The initial `autoSize` implementation hugged content on a *single line*, so a long thought sprawled into a mile-wide card — wrong. Dropped `autoSize` entirely (removed the field + branch). Two resize behaviors owner named are both served: auto-height as you type; manual width by drag. Verified live in-browser (old 220px note now wraps to 4 lines; new note wraps at 240). _(owner feedback + Claude)_
- **D-054 — Knowledge-layer research done → `research-knowledge-layer.md`** (cited; the Phase-2 design input). Actionable calls: **tags stay flat** (Capacities' type/tag/collection split ≈ our `kind`/`stage` vs `tags`; don't nest until a merge/rename tool proves it needed). **Linking without `[[]]` = two gestures to the same `links` row:** (a) a tap/`@` **search-picker** (Heptabase/Notion) everywhere; (b) a **bespoke SVG drag-to-connect overlay** on `react-rnd` positions in canvas mode — copy tldraw's *binding model* (anchor / precise / reroute-on-move), **NOT** the tldraw library (not in the approved stack). **Topic-pages = a query-time view** over the existing join tables (no new table). **Graph = local-neighborhood first** (global graphs turn to hairball past ~200 nodes) via **`react-force-graph`** (best React fit; needs dep approval). **Growth mechanic = no new feature** — the planned backlinks panel on the bit detail page + `stage` maturity IS the visible lineage (Matuschak "revise in place"). _(research + Claude)_
- **D-053 — Roadmap REPRIORITIZED around the owner's north star ("Obsidian + an interactive canvas") after a fresh-eyes plan review.** New canonical sequence (in `ROADMAP.md`): **cloud+login+backup → the knowledge layer (tags · backlinks · forward-links · find · topic-pages — built beautifully + *with* real content) → graph view → richer boards + growth mechanic → (later) phone capture → browse/resurface feed → privacy gradient**. Adopted from the review: **login wall is Phase-1 non-negotiable** (don't put real notes on the public deploy); **"grow" must be a real *feature*** (the connect/tag/graph layer *is* it — topic-page as a *living surface* + link lineage); **build the knowledge layer *with* seeded content, not before it**; **success metric = "do I open it unprompted after ~3 weeks to connect/develop?"** (not "does compose feel good"). Owner's own calls: knowledge layer = **highest value, do it beautifully** (research pass greenlit); **capture + feed = later** (matter with volume); **migration = manual** (Apple Notes won't auto-export cleanly — no importer built); **rotation deprioritized**. _(owner + fresh-eyes review + Claude)_
- **D-052 — ✅ PEN CONFIRMED WORKING ON THE DAYLIGHT (real stylus).** After the canvas fix (D-051): draw → lift (no crash) → Done → resize → move all work; owner: "everything basically looks perfect." The whole handwriting vision is validated on-device. **Aside — the `ERR_CERT_COMMON_NAME_INVALID` scare was NOT the app/deploy:** the Daylight's browser was dropping **SNI**, so Vercel served its `no-sni.vercel-infra.com` fallback cert (name ≠ `world-website.vercel.app`). Diagnosed by comparing the served cert with vs without SNI. **Fixed by fully closing + reopening the browser** (stuck connection state). Filed: if it recurs, restart the browser. _(owner Daylight test + Claude)_
- **D-051 — Second Daylight pen bug: crash-on-pen-lift was the RENDERING METHOD, not the palm (that fix held).** Owner confirmed: palm no longer marks + stylus draws, but lifting still crashed. Root cause: the draw overlay rebuilt the *entire drawing as SVG DOM on every pointer sample* — dozens/sec — and the tablet's lighter browser couldn't sustain the layout/allocation churn; the renderer crashed as the stroke peaked (desktop's power masked it). Decisive clue: the validated pen **spike drew on a `<canvas>`** and ran fine on the *same* Daylight → the variable is canvas vs regenerate-SVG-per-frame. **Fix (`draw-overlay.tsx`): live drawing now on a `<canvas>`** (rAF-throttled, `Path2D` fill — cheap, matches the spike); strokes still kept as vectors; the finished doodle bit is still crisp SVG (rendered once). Verified end-to-end on desktop (canvas draws → Done → doodle bit); redeployed. Owner re-testing on Daylight. _(owner Daylight test + Claude)_
- **D-050 — First Daylight pen test surfaced two bugs — found, fixed, redeployed.** (1) Resting a palm on the screen drew marks. (2) Lifting the pen **crashed the tablet browser** ("this page could not load"), losing the un-finalized drawing. Root cause (one, not two): palm + stylus fire as **competing simultaneous pointers**, and the tablet choked on the tangle (desktop never hit it — a mouse is one clean pointer). Fix in `draw-overlay.tsx`: draw only from **pen/mouse** (ignore `pointerType === "touch"` = palm/finger), **one active pointer at a time**, plus a non-finite-coordinate guard and `pointercancel` handling. Typecheck clean, redeployed. Safe for the stylus because it reports `"pen"` (the spike proved it gives pressure). Finger-draw stays a deliberate "later." _(owner Daylight test + Claude)_
- **D-049 — Compose prototype DEPLOYED to Vercel — LIVE + public: https://world-website.vercel.app** (homepage `/` → `/compose` via a proxy redirect added for the prototype). Deployed with `vercel --prod` (owner authed as `siva-sakti`, scope `gargis-projects`). **No cloud DB wired** — the board is localStorage, **per-device** (no cross-device sync yet; that's the DB step). **No auth/privacy layer yet** — reachable by anyone with the URL; owner OK for now (nothing sensitive on it), add the private/public lock before that changes. Prod build green (`○ /compose` prerendered static). **Custom domain pending** (owner's, via Vercel dashboard → Domains + DNS). **Unblocks the big one: on-device Daylight testing of the whole compose surface — especially the pen with a real stylus.** _(owner + Claude)_
- **D-048 — Infinite canvas = a LATER "camera" layer; today's coordinate math is forward-compatible (owner asked if it breaks the pixel math — it doesn't).** Infinite pan/zoom will be a CSS `transform: translate() scale()` on a container holding all the bits (the tldraw/Freeform approach from `draft-research-canvas.md`). Bits already live in a **screen-independent coordinate space**, and doodle strokes are stored **relative to their own bit** — so the camera never touches bit/stroke math; a doodle just moves/scales/zooms with its bit. The *only* change when we add it: the client→space conversion at capture points (new-note position, image-drop position, pen stroke points) gains a pan offset + zoom divisor — I'll centralize those into one `screenToWorld()` helper. `react-rnd` `scale` prop wired to zoom (D-030). **Sequencing:** add the camera *after* the bit types + interactions are solid on the fixed board (nearly there) — it drops in as a wrapper, no rewrite, nothing built now gets undone. _(owner raised + Claude)_
- **D-047 — Text corner-scale BUILT (v1), owner chose the Freeform "drag a corner" over an A−/A+ control (D-045's open question, resolved).** A distinct **filled-blue handle at the bottom-right** of a text note; dragging it scales width + font size by one factor (top-left anchored, custom pointer handler — *not* react-rnd's corner, so no auto-height fight), so the note zooms while the wrap stays put. Padding switched to `em` so it scales too. `fontSize` stored on the bit. **v1 = one corner (bottom-right)**; extend to all four corners if the feel lands. Render + typecheck verified; **drag-feel is owner's to confirm** (didn't self-test by dragging, to avoid mutating the owner's live, un-backed-up board). _(owner + Claude)_
- **D-046 — Rich-text vision (owner): the text bit should grow into a full rich-text editor** — headings, bold/italic/underline, lists, tables, inline media, links, maybe multiple fonts. **Feasible — already on Tiptap:** StarterKit gives bold/italic/headings/lists/quote/strikethrough today (even via ⌘B), and underline/links/tables/inline-image are official add-on extensions. Needs a **formatting UI** (selection *bubble menu* + toolbar) → folds into Task 4. **Tension to flag:** multiple font *families* fights the "one typeface, quiet" stance (D-008) — bold/headings/italic are expressive within one face; distinct fonts are a deliberate departure = owner's explicit call. Inline media that text flows *around* = the D-044 wrap dream (later). _(owner + Claude)_
- **D-045 — Text resize = two gestures (Freeform model); the reflow half is BUILT + verified, refining D-040.** Owner named the confusion exactly: *"reshape the box"* vs *"scale the whole thing"* are different operations. Split by handle: **side handles (left/right) = reflow** (change wrap width → text re-wraps; height follows the text, so there are **no top/bottom/corner handles** — which removes the earlier jumpy snap); **corner handles = scale** (images already do this, aspect-locked; text = TBD). Reflow verified in-browser (side-only dots render; `tsc` clean) without disturbing the owner's live board. **Open — owner's choice: how to scale *text* — drag-a-corner-to-scale-the-font (Freeform feel) vs a size control (A−/A+, always crisp & precise).** Supersedes D-040's "every handle reflows" (that was the buggy version). _(owner + Claude)_
- **D-044 — Text-flow *dream vision* recorded (parked / someday).** Owner's north star: text should eventually **flow around arbitrary forms/shapes** (magazine / Freeform-style inline wrap), not be confined to its own rectangle, with elements able to sit under/around it. This is the existing **"wrap-box"** later item; the **MVP text bit stays a simple rectangular reflow box**. Not near-term — captured so it isn't lost. _(owner)_
- **D-043 — Image bit built (compose Task 2, minus crop).** Drop an image onto the board, or "+ image" (file picker) → the file is **downscaled** on import (canvas → JPEG, ≤1400px longest edge, q0.82 — keeps localStorage sane) → placed as an **aspect-locked** image bit at the cursor (default ~320px display width). Renders filling the card (no paper padding, clipped to the rounded frame); resize preserves aspect (`react-rnd` `lockAspectRatio`). Non-image files and read/decode failures are ignored (never throw into the drop handler). Verified end-to-end via a real simulated drop + typecheck. **Crop deferred:** non-destructive crop (D-016) likely needs a new dep or a hand-built cropper — owner's call, so not built. _(Claude)_
- **D-042 — Visible resize handles: subtle dots on the selection frame's four corners** (`react-rnd` `resizeHandleStyles`), shown only when a card is selected-not-editing. Makes the owner's "easy to see the edges so you can resize" literal, and gives touch (Daylight) a real grab target vs. invisible edges. Verified in-browser. _(owner feedback + Claude)_
- **D-041 — Card interaction = click-to-select-then-edit two-step; root-caused a real bug, fixed at the cause not the symptom.** Symptom: a *single* click jumped straight into text-edit, which also hid the resize frame (frame only shows when `selected && !editing`). Root cause (found by reading the code, not guessing): react-rnd's `onDragStart={onSelect}` fires on pointer-*down*, pre-selecting the card, so the pointer-*up* click saw `selected===true` and chose edit. Fix: select on the inner div's pointer-down and gate edit on *"was it already selected before this click"* via a ref; removed `onDragStart`. New notes still open in edit mode (creation sets `editingId`). Verified: click→select (frame, no caret), click→edit (caret). _(Claude; owner-directed "diagnose where it is, then build — don't patch and create debt")_

### Review-driven (external evaluation, then reconciled with owner)

- **D-017 — Build sequence inverted to CAPTURE-FIRST.** Foundation → cloud deploy → capture loop → browse+resurface → data-safety → tagging → compose. The daily loop (capture + return) ships before the sit-down compose tool. _(review F1 + owner)_
- **D-018 — Ontology: the fragment is the atom; placement-optional.** Capture creates a *bare* fragment (no board, no placement); a fragment can live on zero/one/many boards. Supersedes D-001. SPEC §0/§5 to be rewritten to match. _(owner)_
- **D-019 — Boards are two-mode:** *collection* (grouped fragments, no spatial layout — quick, any device) → *canvas* (same fragments given positions, arranged spatially — sit-down). Position on a placement is optional. _(owner)_
- **D-020 — Two tag roles:** **topical tags** = open, growable, *linkable topic-nodes* (each a page collecting everything about it = Obsidian-style backlinks, tap-not-type UX) — these are the connective/graph web. **Categorization** = `kind` (fragments) + `stage` (boards), fixed enums. _(owner)_
- **D-021 — Data safety:** soft-delete/trash for fragments; automated export/backup from day-one-of-real-data; export belongs *in* the build queue (was a spec contradiction). _(review F4 + owner)_
- **D-022 — Tagging at capture = optional + fast, never required to save.** Refine while tending; images may be tagged at capture or later. _(owner + review F7/F8)_
- **D-023 — Daylight is a first-class compose device** (touch, **landscape-only**); phone = capture + browse only. **Rotation cut from v1** (no approved lib provides it). **Graph view demoted** to later/scoped (backlinks deliver the value; the topical web is the graph). _(owner + review F6/F9)_
- **D-024 — `kind` is optional, no default, set at tending;** build no filters on it until real data shows it's assigned. _(review F8 + owner)_
### Feasibility-review-driven (second external review: "will it build / will it be the thing")

- **D-025 — Capture transport:** media uploads go **client → Supabase Storage directly**, never through a Vercel function (4.5 MB serverless body cap vs 3–8 MB iPhone photos; server actions cap at 1 MB). App-wide rule. _(review A1)_
- **D-026 — iOS capture auth = bearer capture-token** (`CAPTURE_TOKEN`) validated by `/api/capture`, which uses the service-role client (narrow, deliberate RLS bypass). Shortcut resizes or exchanges for a signed upload URL — decided in the spike. _(review A1)_
- **D-027 — Offline outbox, honest guarantee:** no Background Sync on iOS → queued captures flush **on next open**; visible "N waiting to sync"; capture shell is a precached client route with a ≤ ~1.5s tap→keyboard budget. Service worker via `serwist` — **new dep, owner approval at phase 2 start**. _(review A2/B1)_
- **D-028 — Init migration amended in place** (nullable x/y + check for collection mode, `kind`, `deleted_at`, HTML-stripped tsvector, rotation column dropped): legitimate because it had only ever been applied to throwaway validation containers — no persistent DB exists. Re-validated against Postgres (unplaced row stored; half-placed row rejected) + clean build. **`stage` deliberately deferred to the boards-phase migration** — its shape (steps/names) is still the owner's open question and boards hold no data until then. _(review A3, nuanced)_
- **D-029 — Backup = GitHub Actions nightly cron** (`pg_dump` + storage sync → private artifact/R2), doubling as the **keep-alive** against free-tier auto-pause. _(review A4)_
- **D-030 — One drag owner per gesture:** react-rnd owns on-canvas drag+resize; dnd-kit owns tray/grid→board pulls; react-rnd `scale` prop wired to canvas scale. _(review A5)_
- **D-031 — Image encode: JPEG on iOS** (Safari can't encode WebP), WebP elsewhere; EXIF orientation honored; buckets + pipeline land in **phase 2** (photo capture drags them in). _(review A6)_
- **D-032 — Both spikes run at phase-2 start:** the one-day end-to-end iOS capture spike (the single biggest risk) and the Daylight pen-feel spike (its outcome reshapes post-compose plans). _(review §4 + B2)_
- **D-033 — Phase-3 grid: the *text* preview card is the primary designed object** (early corpus is mostly text); phones render boards in **collection mode**; phase 5 adds an "untagged recent" strip on home. _(review B3/B4/B5)_
- **Schema gap (remaining):** only `stage` — added in the boards-phase migration once the owner names the steps.

## Needs owner (blocked on you; not touched while you're away)

- **To run it live (one-time):** your disk is at **97% (~15 GB free)**, and `supabase start` pulls **~6–8 GB** of Docker images, so I did **not** do that to your machine while you were out. When ready (free some space if you can):
  1. `npx supabase start`  (boots local Postgres/Auth/Storage; first run pulls images)
  2. `npx supabase db reset`  (applies the migration + creates buckets once seed/config is set)
  3. `node scripts/create-owner.mjs`  (creates your login from `.env.local`)
  4. `pnpm dev`  → open the URL, sign in with `OWNER_EMAIL` / `OWNER_PASSWORD`
  Then the create-canvas / add-text loop is live. (I left `supabase/config.toml` at its known-good default rather than trimming services blind, since I couldn't test a trimmed config — happy to slim it to shrink the image pull once we can verify it.)
- **Storage buckets** (`public`, `private`) aren't created yet — they land in **phase 2** (photo capture pulls in the whole image pipeline, D-031).
- **`serwist` (service worker) needs your approval** at phase 2 start — required for the offline outbox (D-027).
- **Both phase-2 spikes need you**: the iOS capture spike runs on *your* iPhone; the pen spike on *your* Daylight. I build the throwaway pieces; you hold the devices.
- **Cloud Supabase project** (URL + anon + service-role keys) — only for real cloud / deploy. Local meanwhile.
- **Vercel + custom domain + any deploy** — left untouched.
- **Image crop = a decision:** non-destructive crop (D-016, and the "crop" in Task 2) likely wants a small dep (`react-easy-crop`) or a hand-built cropper. I deferred it rather than add a dep unasked. Drop + resize work now. Tell me which way and I'll build it.
- **No "delete a bit" yet:** the board can't remove a card (wasn't in Task 1/2 scope). Small + genuinely needed for real use — I'll put it at the top of the next build unless you say otherwise. (Your throwaway test scribbles persist until then.)
- **Any dependency beyond the approved list** — I'll defer the feature and list it here rather than install.
- **Review checkpoint:** the full `SPEC.md` is written — worth a read to confirm I captured the whole vision before the canvas gets deep.

## Open questions (parked, not urgent)

- Mobile: how much spatial arranging, if any, on the phone (default: capture-linear on phone, arrange on desktop).
- Revisiting at scale: does the feed suffice or do tags/graph carry it (owner: don't worry about it for now).
- v1 cut line vs the full queue above.

## Work done

**Pen (Task 3) — DONE, owner-verified on desktop ("works great"); Daylight stylus/pressure test pending deploy:**
- `perfect-freehand`→SVG (pre-approved dep, the Daylight-validated feel). `✎ pen` → a full-board draw overlay captures strokes live as a filled-outline ink (`#1c1813` on paper) → **✓ done / ✕ cancel**. Modular: `stroke.ts` (path + bounds), `draw-overlay.tsx`. Typechecks clean.
- **Model (owner-confirmed):** the pen-session between tapping the pen and tapping **Done** = ONE doodle bit. No per-pen-lift, no proximity-grouping (too complex) — just Done finalizes. A finished doodle is a normal bit (drag/resize/select like text/image). Different pen types/colors = later.
- **Step 2 DONE + owner-verified on desktop:** ✓ Done bundles the session's strokes into ONE movable doodle bit — `finishDoodle` computes the bbox, stores strokes relative to the bit; `doodle-bit.tsx` renders SVG with a native-size viewBox so the ink scales crisp; transparent card (just floating ink), aspect-locked corner resize; persists on reload. Owner: "works great."
- **Daylight (touch + real stylus pressure) NOT yet tested — deploy-gated** (LAN dev doesn't hydrate on the Daylight). The pen engine is the same `perfect-freehand` (pressure-aware, `simulatePressure:false`) that was validated on the Daylight in the phase-2 spike, so confidence is high — but it needs a real hosted URL to confirm on-device.
- **⚠️ Turbopack dev gotcha (cost ~an hour):** new `globals.css` rules can compile clean yet never reach the browser CSSOM — computed styles show defaults despite the rules being on disk. It's **not** a selector/specificity bug; Turbopack served a stale CSS chunk. **Fix: restart dev server + `rm -rf .next`.** Verified via CSSOM inspection, not by guessing.

**Compose MVP prototype (`/compose`, localStorage) — Tasks 1–2 done + verified in-browser:**
- **Task 1 — board + universal card + text bit.** `/compose` client route (no cloud; the auth proxy early-returns for `/compose` so it loads with no DB). One universal draggable/resizable card (`react-rnd`) reused by every bit type; text content = Tiptap. Create via "+ note" or double-tap empty. Text box opens at a general width (240px), **wraps + grows downward**; drag an edge to change width, text re-wraps (D-040 — dropped the old single-line "hug"). **Click selects** (frame + 4 corner handles, D-042); **click-again edits** (D-041 — fixed a real bug where a single click skipped to edit and hid the resize frame). Persists across reload.
- **Task 2 — image bit (minus crop).** Drop onto the board / "+ image" → downscaled, aspect-locked image card (D-043).
- **Verified in-browser** (localhost, Chrome): create/edit/hug/persist, the select→edit two-step, side-handle reflow (D-045). `tsc --noEmit` clean. **Image bit confirmed by the owner** dragging in real JPG/PNG photos (add/resize/drag, live retreat board).
- *Honest correction:* my earlier "image verified end-to-end" was only a **code-generated PNG via a synthetic drop, not a real file**. The real file-picker path fails **silently on HEIC** (Chrome can't decode Apple's HEIC — patent-locked HEVC; Safari can) → now a to-do (surface a message; conversion later).
- **Gaps for next:** no delete-a-bit yet; unreadable-image message (no silent fail) + HEIC support; text *scale* gesture (D-045, owner's choice); crop (dep decision); Daylight touch is deploy-gated (LAN dev doesn't hydrate there).

**Research + spike pass (compose-first prep) — done:**
- `draft-research-canvas.md` — studied JSON Canvas / tldraw / Heptabase / Excalidraw. Conclusions: board = **DOM cards in a CSS-transformed container** (tldraw's approach, gets Freeform-smoothness + rich text); keep `bits`+`placements` (richer than JSON Canvas — it has no shared-card-across-canvases); **JSON Canvas's `group` node resolves the open "is-everything-a-bit?" question → allow a few non-bit structural elements**; edges→links; perfect-freehand→SVG pen (vector, tiny). Optional JSON Canvas export for interop.
- **Pen-feel spike → tested on the Daylight: WORKS.** Owner: "a great basic pen, does the trick, working perfectly." **→ in-app web-pen handwriting is a VIABLE path (D-037 validated) — we build handwriting into the app; Freeform not needed for it.** Bugs found + fixed during testing: mobile disappear-on-lift (→ strokes-as-vector-data, redraw, dropped `desynchronized`), weird stroke-start cap, and black-ink-invisible-on-dark (→ theme-aware ink). https://claude.ai/code/artifact/406f1d0f-0d9d-4d6e-a5e1-3c23d36b48e5 _(Learnings for the real pen: perfect-freehand→SVG vector = tiny + robust; store strokes as data not canvas pixels; theme-aware ink.)_

**Foundation (queue item 1) — done; builds + typechecks clean:**
- Next.js 16 + React 19 + TypeScript strict + Tailwind 4, scaffolded into the repo.
- `supabase/migrations/20260715000001_init.sql` — full schema (canvases, bits, placements, tags, bit_tags, canvas_tags, links) + `updated_at` triggers + FTS tsvector + indexes + RLS (owner-all; anon default-denied). **Validated by applying it to a throwaway `postgres:16-alpine` container — clean.**
- `src/lib/supabase/{client,server,admin}.ts` — browser / server / service-role clients.
- `src/proxy.ts` + `src/lib/supabase/proxy.ts` — auth session refresh + route guard (Next 16 proxy convention).
- `src/lib/types.ts`, `src/lib/storage.ts` (single storage access point), `src/lib/db/{canvases,bits}.ts`.
- Auth: `/login` (Supabase Auth email+password) + sign-out.
- Quiet base layout + globals (white, one typeface).
- `/` — list + create canvases, sign out, empty state.
- `/c/[id]` — canvas view rendering placed bits + add-text.
- `scripts/create-owner.mjs` — idempotent single-owner setup. `.env.example` + `.env.local`.

**Canvas (queue item 2) — started:** static render + add-text works. Drag / resize / rotate (dnd-kit + react-rnd), undo, and viewport scaling are **intentionally deferred until the DB is running**, so they're verified rather than guessed (no-debt).
